# Debt register

Known gaps. IDs are **append-only and never reused** — a closed item keeps its
number and its row, marked closed.

**Highest ID issued: D72**

## Open

| ID | Item | Impact if unresolved | Blocks | Opened |
| D1 | **`dsa-concepts` has no GitHub remote** — 13 verified articles, 118/118 code blocks, 121/121 links, 539/539 tests, all sitting in a local repo only. Registered as `PlannedRepoId`; no adapter; produces no articles. Cross-corpus refs to it warn rather than fail. | The suite's only fully-verified corpus is absent from the site. Its benchmark methodology — median of 11 trials after 10 warmup calls, adversarial inputs alongside random — is the strongest single claim in the whole body of work and currently has no reader. | Nothing. Resolve by creating the remote, pushing, tagging `v1.0.0`, then moving `dsa` from `PlannedRepoId` to `RepoId`, adding its adapter spec, and submoduling it. | 2026-08-15 |
| D4 | **Default branches verified** (session 1). `main`: nextjs, nestjs. `master`: react, angular. GitHub name for React is `react-concepts`; the mount is `content/react`. `auth`/`authz`/`websec` are not corpora and carry no entry (session 1 follow-up). | Silently 404ing "View source" links if `REPO_DEFAULT_BRANCH` / `REPO_ORIGINS` drift. | — | 2026-08-15 |
| D5 | **`description` frontmatter — one selected article still missing it.** Required field, no fallback. The Q1 pass has landed in all four corpora: `nextjs@v0.3.0` 10/10, `react@v0.5.0` 58/73, `angular@v0.3.0` 93/94, `nestjs@v0.3.1` 20/20. Measured 2026-08-17: **181 of 197 adapt**. The single remaining `description` miss is `angular docs/recipes/elements/widget-deployment.md`, which is the duplicate file tracked as D15; the other 15 failures are D11's untitled `react` articles, which the pass skipped because naming an untitled article was not something to invent. Templates that omit the key (and would reintroduce this) are D16. | `verify-frontmatter` exits 1 on 16 files. It no longer blocks the catalog — `build-catalog` emits with exclusions, and unresolved refs (D13) are recorded rather than refusing the write. | Phase 1 item 16; remaining misses close with D11 and D15; template recurrence is D16 | 2026-08-15 |
| D7 | **`react-concepts` anchor slugs unverified** for articles 27–36 and recipes 5–10. Session 2 added `extractSections()` with a GitHub-slug algorithm verified against real anchors in `error-boundaries.md`, but the specific numbered articles/recipes named here have not been individually diffed against it. | Broken in-page cross-references. | A pass running `extractSections()` against every `react` article and diffing anchors used in `related`/inline links | 2026-08-15 |
| D8 | **`angular-concepts`** — 6 of 23 Phase 2 articles outstanding; `attribute-directives` is a stub. | Six articles absent from the site. | Angular Phase 2 | 2026-08-15 |
| D9 | **Demo labs have no home** — `auth`, `authz`, `websec` are working demos with nowhere to appear. ADR-0002 proposes deploy + iframe under `/en/demos/*`. | The one subject area with no article coverage stays invisible. | ADR-0002 decision | 2026-08-16 |
| D10 | **`demo-attacked-web` is deliberately vulnerable.** If deployed on a subdomain sharing the `.nxhhuy.tech` cookie domain, an XSS demo could read the main site's session cookie. | A demo becomes a real vulnerability. | Before any deploy of that app | 2026-08-16 |
| D11 | **15 `react-concepts` articles have no title at all** — neither a frontmatter `title` key nor an H1 in the body. Session 2 audit found 14; the session 2 follow-up found the 15th, `rendering/react-compiler-deep-dive.md`, which the old regex-based `deriveTitle` had been silently titling `TypeScript projects also need the Babel core types:` from a line inside a fenced code block. Full list: `concurrent/actions.md`, `concurrent/concurrent-rendering.md`, `concurrent/suspense.md`, `concurrent/use-and-promises.md`, `ecosystem/data-fetching-tanstack-query.md`, `ecosystem/routing-react-router.md`, `ecosystem/state-management-landscape.md`, `ecosystem/styling-approaches.md`, `ecosystem/testing.md`, `forms/forms-at-scale.md`, `rendering/react-compiler-deep-dive.md`, `server/server-components.md`, `server/ssr-and-hydration.md`, `recipes/data-fetching/strictmode-double-mount.md`, `recipes/data-fetching/request-waterfall.md`. | These 15 articles cannot adapt even after the description pass (item 16) lands. | Corpus-side PR adding an H1 to each, then re-tag `react-concepts` | 2026-08-16 |
| D15 | **`angular-concepts` `docs/recipes/elements/widget-deployment.md` is a duplicate of another article, filed under an unrelated slug.** Verified 2026-08-17: its body is byte-identical to `docs/concepts/tooling/cdk-coercion.md` apart from the relative depth of four link paths, its H1 is `Input Coercion: built-in transforms and CDK utilities`, and its frontmatter carries only `recipe_id`, `angular_baseline`, and `status` — no `description`, which is why it is the one selected article the Q1 pass did not cover. Previously tracked only inside a D5 row and never given an ID of its own. | The single remaining `description` failure, and if it were ever given one it would publish the same article twice under two URLs. | Corpus-side in `angular-concepts`: either write the real `widget-deployment` recipe or delete the file, then re-tag | 2026-08-17 |
| D16 | **`ARTICLE_TEMPLATE.md` and `RECIPE_TEMPLATE.md` omit the required `description` key** in both `nextjs-concepts` and `angular-concepts`, so every article authored from them reintroduces D5. Confirmed 2026-08-18 against the current pins (`nextjs@v0.3.1`, `angular@v0.3.1`; re-verified 2026-08-26): `docs/templates/ARTICLE_TEMPLATE.md` and `docs/templates/RECIPE_TEMPLATE.md` in each repo list frontmatter without `description`. **`react-concepts@v0.5.0` and `nestjs-concepts@v0.3.1` have no article/recipe templates** — no `ARTICLE_TEMPLATE.md`, no `RECIPE_TEMPLATE.md`, no `docs/templates/` directory, and no mention of either filename. Confirms the nestjs agent's report. Those two cannot reintroduce D5 via this path; they also have no shared template to add `description` to. | The next article copied from either nextjs or angular template ships without `description` and fails `verify-frontmatter`. | Corpus-side in `nextjs-concepts` and `angular-concepts`: add `description` to both templates, then re-tag. Not a `corpus-web` adapter change. | 2026-08-18 |
| D17 | **Corpus repo gates are uneven.** `react-concepts` — no `package.json`, no CI, nothing. Largest corpus at 77 markdown files, holds all 15 of D11's untitled articles, and is the source of the first course. Every fix there has been verified by hand. **Urgent.** `nestjs-concepts` — manifest and one verify script, no workflow. `verify:forbid-unknown-values` exists and nothing runs it, so the probe committed to make D6 re-runnable is still only run by hand. **Cheap** — copy `verify.yml` from a sibling. `nextjs-concepts`, `angular-concepts` — both have `package.json` and `verify.yml`. Fine. | Corpus defects reach `corpus-web` as catalog exclusions or unresolved refs instead of failing in the source repo. | Per-corpus CI in `react-concepts` and `nestjs-concepts` | 2026-08-19 |
| D19 | **Site CI gates missing:** `verify-a11y`, `lighthouse-ci`, Playwright screenshot diffing on both article routes. Two grid bugs shipped past every existing gate during POC work and were caught only by a human opening the page. Tension to resolve when picked up: axe's WCAG 2.2 `target-size` rule fails the rail's 18×2px ticks, so "axe clean" and "matches the POC" may be mutually exclusive. | Visual and a11y regressions ship. CI already names these scripts; they do not exist. | Phase 1 item 13 gates in `roadmap.md` §13 | 2026-08-19 |
| D20 | **Shiki** (Phase 1 item 10). The POC hand-rolled highlighting with `.k` / `.st` / `.fn` spans. Dual-theme Shiki under Cache Components is its own decision. **Closed 2026-09-03** by `polish/d20-shiki-buildtime` off develop (PR #153, squash `46b77e6`): build-time syntax highlighting via `rehype-pretty-code@^0.14.5` + `shiki@^4.4.3` (both in `apps/web/devDependencies` — zero client bundle impact). Two custom Shiki themes at `apps/web/lib/shiki-theme-{dark,light}.json` hand-map every token scope (comment / string / keyword / function name / class name / variable / etc.) to the existing `@theme` tokens in `packages/ui/src/tokens.css` — dark mode uses `--color-signal-soft` (#F2C782) for strings / class names, `--color-cool` (#6AA9D8) for keywords, `--color-display` (#E7EDF4) for function names; light mode darkens the same hues for 4.5:1 contrast against parchment. NOT a stock GitHub Dark / Light theme, per the user directive in session 161. `onVisitLine` callback adds `'line'` className to every Shiki line span; CSS `counter-reset` + `counter-increment` on `.av-cb pre code > span::before` produces the line-number gutter with zero JS. Dual-theme via `--shiki-dark` / `--shiki-light` CSS custom properties Shiki emits inline on every token span; `[data-theme='light']` override reads `--shiki-light` instead. Build-time highlighter runs once per build (or once per first-access cache hit via the existing `renderArticleMarkdown` `'use cache'` + `cacheLife('max')` wrap); for 196 articles that's 196 highlight runs total, then nothing. All 9 gates green. Live probe `/en/blog/nextjs/cache-components-model` returns 25 Shiki figures with 2702 `--shiki-dark` CSS variable tokens + 368 `class="line"` markers. **Side effect**: Pagefind index word count dropped 28822 → 26944 (−1,878 words, ~6.5%) because Shiki wraps each token in its own `<span>`, fragmenting text into smaller chunks for the indexer; search still works, some excerpts may be slightly shorter. **Caveat**: plain text code blocks (no language tag) fall through to the old `av-ln` line splitting — visually a single line of monospace text with the line-number gutter, no syntax highlighting. Correct: there is no language to highlight. Out of scope to add a heuristic "guess the language" pass. | Every code block on article / lesson routes now renders with token-level syntax highlighting matching the site's palette, dual-theme-aware via the existing cookie + data-theme pipeline. | Phase 1 item 10 | 2026-08-19 |
| D21 | **Pagefind + `⌘K`** (Phase 1 item 11). **Closed 2026-08-31** by `polish/d21-pagefind` off develop. Pagefind 1.5.2 declared in `apps/web/devDependencies`; `postbuild` hook indexes `.next/server/app/en/**.html` into `apps/web/public/pagefind/` (221 pages, 28822 words, build-time only); native `<dialog>`-backed `<SearchDialog>` mounted at the `[locale]/layout` level; `<SearchTrigger>` button replaces the disabled `SearchPlaceholder` at the topbar's right edge. Opens on ⌘K / Ctrl+K; debounced 80ms queries; up to 8 results with Pagefind excerpts; ArrowUp/Down + Enter navigation. Bracket `[param]` placeholder shells (Cache Components fallback) skipped by Pagefind because they have no `<html>` element — exactly the failure mode `verify:prerender` already excludes. Replaced the `.srch` `<label>` chrome with `.srch.srch-trigger` `<button>`; a11y-correct (`aria-keyshortcuts`, `aria-label`, native Esc handling). | n/a — D21 closed | n/a | n/a |
| D22 | **SEO residue** (Phase 1 item 14 minus what listing routes ship): sitemap, `robots.txt`, OG image generation to `cdn.nxhhuy.tech`. **Partially closed 2026-08-31** by `polish/d22-seo-residue` off develop: `/sitemap.xml` (App Router route handler emitting 219 URLs: 1 locale × (3 listing surfaces + 2 course details + 18 lessons + 196 articles)) and `/robots.txt` (`User-agent: *`, `Allow: /`, `Disallow: /api/`, `Sitemap:` pointer) — both reuse `getCatalogView()` so they cost O(1) at build time and ship as static text. `.gitignore` updated to ignore `apps/web/public/pagefind/` build output. **Closed 2026-09-03** by `polish/d22-static-og` off develop (PR #152, squash `9bbd2f1`): static single-design `/opengraph-image` route at `apps/web/app/opengraph-image.tsx` via `next/og`'s `ImageResponse` (Satori), 1200×630 PNG, mirrors the home-hero palette discipline (`--color-ink` ground + warm `--marketing-accent-bloom` upper-right + cool `--color-cool` lower-left). Fonts bundled at `apps/web/public/og-fonts/` (Archivo-Bold 111KB + IBMPlexMono-Regular 133KB) — Satori requires real `.ttf` bytes, gstatic URLs are build-hashed and 404 across versions. `og:image` + `twitter:card` metadata wired into all 5 surface `generateMetadata` functions (home / blog index / blog article / course detail / lesson) via shared constants in `apps/web/lib/site.ts`. No new npm dep (`next/og` bundled with Next 16.3.1). All 6 gates green; live probe returns HTTP 200, `image/png`, 1200×630, 85.9KB. **The `cdn.nxhhuy.tech` half stays deferred** — DNS + Vercel project routing for the CDN sub-domain remains a user-action item; the local `/opengraph-image` route is the canonical URL for now and works on Production without any dashboard config. PR #97 (Polish-6) and PR #96 (Polish-5 batch-5) roles already documented in their corresponding SESSION-LOG entries. | `/sitemap.xml` (219 URLs) and `/robots.txt` ship as static text; `/opengraph-image` ships as a single static 1200×630 PNG; every adapting page emits `og:image` + `twitter:card` metadata. | Phase 1 item 14 | 2026-08-19 |
| D24 | **Interactive layer, tier 1** — corpus-agnostic primitives, blocking: build-output tab group, quiz, flashcards, code-assembly exercise (§7.5), stepped-diagram shell. The POC's tab group has `role="tablist"` with no `aria-controls`, no `tabpanel` roles and no roving tabindex; the exercise chips are `div`s unreachable by keyboard. Fix when built. The home demo panel is an inert placeholder. | Home and lesson pages show coming-soon slots instead of interactives. | Session 4 / Phase 3 | 2026-08-19 |
| D25 | **`/en/license`** — item 13's second half. The sole carve-out to the no-personal-content rule, since CC BY 4.0 requires naming a copyright holder. **Closed 2026-08-31** by `polish/d25-license-page` off develop: new `apps/web/app/[locale]/license/page.tsx` (RSC, prerendered for every registered locale, CC BY 4.0 summary + per-surface notes for code samples and adapted articles + link to creativecommons.org + `mailto:` block for re-use); new `apps/web/components/chrome/site-footer.tsx` (first site footer — same email contact + inline `/[locale]/license` link); `apps/web/app/[locale]/layout.tsx` mounts `<SiteFooter>` once per locale; `apps/web/lib/routes.ts` adds `licensePath()`; `apps/web/messages/en.json` gains a 15-key `license.*` namespace plus `nav.license`. The `nxhhuy@gmail.com` email carve-out lives ONLY on this page and in the footer (per session protocol). All 3 gates green: typecheck 5/5, next build 236/236 + new prerendered `/en/license` route, verify:prerender 196/196+18/18, verify:frontmatter 196/196. | `/en/license` ships at `/en/license`; site footer renders on every locale page with the same email + license link. | Phase 1 item 13 | 2026-08-19 → n/a |
| D26 | **Accounts and progress sync.** Progress is anonymous `localStorage` (article routes). Migrating it into a real account on first sign-up is roadmap §10 and needs the Nest backend. Sign-in is omitted from the nav. **First slice landed + MERGED 2026-09-15** (sessions 194 scaffold → 195 doc/skill audit → 196 `req.login()` session-persistence fix, PR #179 merged via squash `aed04ee` onto `develop`): `apps/api/src/modules/auth/` (8 files) — `GET /auth/google` → 302 to Google, `GET /auth/google/callback` → cookie set + 302, `POST /auth/logout` → 204, `GET /me` → 200/401; Postgres-backed sessions via `express-session` + `connect-pg-simple@10` (lazy `corpus_session` table on first `set()`); `users` table migration (uuid PK, `google_sub` UNIQUE, partial-unique `email`); single `<SignInButton>` on the web topbar pointing at `${NEXT_PUBLIC_API_URL}/auth/google`. `pnpm verify:api-runtime` 10/10 PASS in auth-enabled mode; all 6/6 PR #179 CI checks green (Repo guards, Content gates, Lint/typecheck/build, A11y/perf, Vercel, Vercel Preview Comments). **Login slice closed** — user can sign in via Google, the session row is written to `corpus_session`, and `GET /me` returns the user. **Second slice NOT yet started**: `POST /progress/migrate` endpoint (apps/web's v1 progress store knows how to send it; no Nest receiver exists yet), avatar dropdown / sign-out button / profile page on web side, refresh tokens, RBAC, CSRF token on `/auth/logout` (documented as out-of-scope in session 194 CHANGELOG entry). **Sub-slice A (popup UX):** landed on `feat/d26-signin-popup-ux` (session 201, PR #184): `<SignInButton>` converted from static `<a href>` to `'use client'` stateful component — opens a centered 520×600 popup via `window.open` to `${NEXT_PUBLIC_API_URL}/auth/google`, polls `GET /me` every 2 s with `credentials: 'include'` while the popup is open, shows disabled "Signing in…" label on 401 (in-flight or within 5 s post-close debounce), reverts on `/me` 200 / user-closed popup / 60 s elapsed timeout. New i18n key `topbar.signInProcessing` (English-only) plus `.topbar-signin--processing` CSS modifier (token-only, no new tokens). Spec at `prompts/session-d26-signin-popup-ux.md`. **Sub-slice A.1 (popup UX bugfixes, session 202):** 4 bugs found by Huy clicking through PR #184's Vercel preview with a real Google account, fixed on the same `feat/d26-signin-popup-ux` branch. (1) State reset on navigation — `processing` moved from local `useState` to a new `SignInContext` (`apps/web/components/chrome/sign-in-context.tsx`), mounted once at `apps/web/app/[locale]/layout.tsx` so it survives `<SiteHeader>` remounts across route changes. (2/2.1) Polling-only success signal replaced with `window.postMessage` from a new flat route `apps/web/app/auth/google/callback/page.tsx`, which the API's success redirect now targets instead of `/`; the `/me` poll interval widened 2s→7s and demoted to a safety net. (2.2) 401-debounce anchor bug — `last401AtRef` was re-stamped on every 401 poll tick instead of only on the popup's open→closed transition, so the 5s post-close debounce window never actually elapsed; fixed by anchoring on a dedicated `popupClosed` state flip. (4) Blanket 60s force-revert `setTimeout` removed — a slow OAuth flow no longer gets yanked back to "Sign in" mid-flight. Architecture calls made autonomously (no new dependency, no Huy escalation needed): state hoist via React Context (no state-mgmt lib in the repo), callback route flat (not a route group, matching D55 convention). Lead code-reviewed the sub-agent's diff and fixed 3 defects before commit: (a) the safety-net poll read `fetch('/me', ...)` — a relative path resolving against the Next.js origin instead of the NestJS API via the `apiUrl` config module, silently breaking the fallback; (b) `authPath` read `process.env.NEXT_PUBLIC_API_URL` directly instead of through `@/lib/config`'s `apiUrl`, bypassing the D55 canonical fallback-to-`''` contract; (c) `aria-label` regressed from the dedicated `topbar.signInAriaLabel` key ("Sign in with Google") to the visible-label key ("Sign in"), an a11y regression. All 3 fixed inline; `revert` also wrapped in `useCallback` to satisfy `react-hooks/exhaustive-deps` cleanly (was a lint warning, not an error, but fixed rather than suppressed). All 9 gates green post-fix: typecheck (web+api) 0 errors, lint (web+api) 0 problems, build (web+api) success — 222 pages / 26929 words unchanged from baseline, `agents:check`/`verify:submodules`/`verify:frontmatter`/`verify:links`/`verify:catalog` all pass (pre-existing content-side warnings only, unrelated to this diff). No Vietnamese, no `vi.json` touch, no `content/` touch. **Sub-slice A.2 (Echo's independent review of A.1, session 202 continued):** Echo (peer reviewer) found 2 further bugs in A.1 that Lead fixed on the same branch/PR. (1) A.1's Context hoist alone did NOT fix Huy's original state-reset repro — root cause misdiagnosed. `<SiteHeader>`'s nav links (`nav-links.tsx`) and logo/pill-CTA (`site-header.tsx`) were plain `<a href>` tags, a real full-document browser navigation that unmounts the entire React tree including `SignInProvider`; Context survives client-side transitions only, not hard reloads. Fix: both converted to `next/link` (zero new dependency, already used elsewhere in the repo for `not-found.tsx`). Confirmed no documented rationale existed anywhere (SESSION-LOG/roadmap/DEBT/rule files) for the original `<a href>` choice — unexamined default from the session-3 scaffold (PR #20/#21), not a deliberate decision. Other raw `<a href>` sites in the app (article breadcrumbs, page-nav, in-body refs in `article-view.tsx`, courses breadcrumbs) are NOT part of this fix — tracked as new debt row D56. (2) Post-close-debounce race: the server writes the session cookie and 302s the popup to the callback route BEFORE that route's own `useEffect` (which posts `oauth-success`) has a chance to run — a real ~100–400ms window observable when popup network is throttled. Closing the popup inside that window meant no success message ever arrived even though login had already succeeded server-side; compounding this, the old 5s post-close debounce was SHORTER than the 7s safety-net poll interval, so the poll structurally never got a chance to catch the late success either. Fix: fire one immediate `/me` check the moment the popup is observed closed, only falling through to the 5s debounce-based revert if that immediate check comes back 401 or errors. All 9 local gates green post-fix (typecheck, lint, build — 222 pages/26929 words unchanged, test 113/113, agents:check, verify:submodules/frontmatter/links/catalog — pre-existing warnings only). Manual empirical click-through against live Google OAuth (popup-network-throttle repro for bug 2, hard-nav repro for bug 1) still not run by Lead — code-level fix + gate suite is the verification evidence; awaits Huy/Echo's own testing. **Sub-slice A.3 (Echo's LIVE click-through of the A.2 Vercel preview, session 202 continued):** Echo actually clicked through this time (not just code review) and found 2 more bugs, both root-caused to source, not just symptom-patched. (6) `GET /me` returned 401 unconditionally regardless of cookie validity — `apps/api/src/main.ts` never called `app.use(passport.initialize())` / `app.use(passport.session())`; `PassportModule.register({ session: true })` only wires DI providers, it does not touch the running Express instance. `SessionAuthGuard` (used directly by `/me`, with no `AuthGuard('google')` in front of it) checks `req.isAuthenticated?.()`, a method Passport monkey-patches onto `req` only as a side effect of `passport.authenticate()` running — which never happens on `/me`'s route. The two OAuth routes worked only because that side effect happened to fire on them. Fix: `main.ts` now calls both `app.use()` calls right after `sessionMiddleware`, before CORS/routes register. No new dependency — `passport` was already a direct `apps/api` dependency. (7) The 7s safety-net poll never stopped after a successful `postMessage` login — `SignInContext`'s postMessage handler called only `setProcessing(false)` on success, with no way to reach into the `<SignInButton>` instance that actually owned the popup and clear its local `pollTimerRef`/`closeWatcherRef`/`popupRef`. Before the bug-6 fix, `/me` always 401'd, so the poll's own success-revert path could never fire either — the poll ran forever until unmount/reload. Fix: button now registers its `revert` callback with the provider on mount (`registerRevert`) and unregisters on unmount; provider calls the registered callback on postMessage success instead of a bare `setProcessing(false)`. All 9 local gates green post-fix (typecheck web+api, lint web+api, build — 222 pages/26929 words unchanged, test 113/113, agents:check, verify:submodules/frontmatter/links/catalog — pre-existing warnings only, unchanged from A.2). Manual empirical click-through of bugs 6+7 specifically still not run by Lead — Echo's own live click-through is the only empirical evidence so far; code-level fix + gate suite is Lead's verification layer on top. **Sub-slice B (avatar + dropdown + sign-out UI, session 204):** landed on `feat/d26-avatar-logout` (PR pending against `develop`, NOT merged). `<SignInContext>` (A.1's state hoist) now owns the `/me` fetch too — provider runs one `fetch(${apiUrl}/me, { credentials: 'include' })` per locale-tree mount, exposes `MeState = 'loading' | 'signed-in' | 'signed-out'` + `me: MeResponse | null` + `refresh()` + a `corpus:auth-changed` `CustomEvent` broadcast, eliminating the slice-B-specific flicker that the A.1 local-state approach couldn't prevent (the per-render `/me` refetch). The 7 s safety-net `/me` poll inside `<SignInButton>` (A.3's standing antipattern that B's prompt specifically called out) deleted along with `pollTimerRef` / `clearPoll()` / `POLL_INTERVAL_MS`; the post-close race fix (A.2) now calls the provider's `refresh()` instead of running its own `fetch()` — the button no longer owns any background timers. New `<UserMenu>` component (avatar `<img>` with `referrerPolicy="no-referrer"` or initial-letter fallback derived from the first non-whitespace character of `me.name`/`me.email`, plus a native `<details>`/<summary> dropdown listing name + email + a plain `<a href="${apiUrl}/auth/logout">` sign-out link — Esc-to-close via a keydown effect); new `<AuthSurface>` client boundary (~30 lines, the wrapper `<SiteHeader>` — a Server Component — needs to call `useSignIn()` from inside); five new i18n keys (`topbar.signedInAriaLabel` with `{name}`, `topbar.signedInAriaLabelFallback`, `topbar.avatarFallbackAlt` with `{name}`, `topbar.userMenuSummary` with `{name}`, `topbar.signOutLabel`) — English-only per spec (no `vi.json` touch). `.user-menu*` CSS block in `apps/web/app/globals.css` reuses the existing topbar token pool (`--color-body`, `--color-display`, `--color-muted`, `--color-graphite`, `--color-surface`, `--marketing-accent-bloom`, `var(--font-display)`) so the avatar chip reads as one chrome element with the `<SignInButton>` it replaces — no new tokens, no Tailwind utilities, no inline styles. Zero new dependencies, backend untouched, no `content/` touch. **D26 stays OPEN** — slice B implements the user-facing surface for already-working `/me`, but `/auth/sign-out` is slice-C scope; until that endpoint exists, clicking the new sign-out link 404s against the unmatched Next route, by design. All 9 local gates green: typecheck (web+api) 0, lint (web+api) 0 (2 surface errors during initial run — unused `locale` prop on `<AuthSurface>` left over from a removed consumer; `eslint-disable-next-line @next/next/no-img-element` referencing a rule not installed on this project's eslint config — both fixed before the green run), build (web+api) 0 (222 pages / 26928 words — matches session-201/-202 baseline), test 113/113 (no new tests added; topbar chrome has no test precedent in `apps/web/test/`, documented in SESSION-LOG Session 204), agents:check, verify:submodules/frontmatter/links/catalog/prerender all pass (pre-existing content-side warnings only). `MeResponse` type re-declared locally in `sign-in-context.tsx` to mirror `apps/api/.../me.controller.ts::MeResponse` rather than imported via `@corpus/api-client` because `packages/api-client/src/` is still an empty stub (OpenAPI SDK not generated yet) and the rule `.cursor/rules/40-web-nextjs.mdc`'s "never hand-write a fetch" only applies once the SDK is regenerated — the existing D26 A-through-A.3 PRs all use the `fetch(${apiUrl}/me, { credentials: 'include' })` direct pattern, so B follows the established convention rather than expanding scope into a CI-side SDK-generation refactor. **Sub-slice B follow-up, PR #185 review fix, commit `13fb600` (2026-09-18):** Lead's review of PR #185 found the shipped sign-out link pointed at a nonexistent `/auth/sign-out` route, framed by invented "slice C" docstrings — but a working sign-out endpoint (`GET /auth/logout`, session destroy + cookie clear + 303 redirect) had already existed at `apps/api/src/modules/auth/auth.controller.ts:145-153` since PR #179; there was no slice C to build, the frontend just pointed at the wrong URL. Fixed: `signOutHref` now `${apiUrl}/auth/logout`; the false "slice C / future POST /auth/sign-out" docstrings in `user-menu.tsx` and `globals.css` rewritten to describe the live route. Separately, the shipped `<details>`/`<summary>` only closed on Escape, contradicting the sub-slice B prompt's explicit requirement (click-outside OR Escape); fixed by adding a `mousedown` document listener (`Node.contains` against `detailsRef`) sharing the same `useEffect` as the Escape handler. **Sign-out is now fully functional end-to-end** — clicking "Sign out" hits the live Nest handler, clears the session cookie, and 303-redirects to `/`; it does not 404. **D26 remaining scope**: profile page, refresh tokens, RBAC, `POST /progress/migrate` endpoint, CSRF token on `/auth/logout` (that last one is a real gap — the endpoint is a bare `GET` with no CSRF protection, exploitable via a forced-navigation link from a third-party page; not fixed by this PR). All 9 gates re-verified green post-fix (typecheck fresh cache-miss on `@corpus/web`, lint 5/5, build 222 pages/26928 words unchanged). | No cross-device progress. | Phase 2 | 2026-08-19 |
| D56 | **Raw `<a href>` navigation outside the topbar/nav chrome remains unconverted to `next/link`.** Found during D26 sub-slice A.2 (session 202): `nav-links.tsx` and `site-header.tsx`'s logo/pill-CTA were converted to `next/link` to fix a sign-in-state-reset bug (hard reload was unmounting `SignInProvider`), but the same raw-`<a href>` pattern still exists in `apps/web/components/article/article-view.tsx` (breadcrumbs, page-nav, in-body reference links) and the courses page breadcrumbs (`apps/web/app/[locale]/courses/[course]/page.tsx`). None of these currently hold cross-navigation React state that would break on a hard reload, so there is no known live bug — this is a consistency/perf debt (avoidable full-page reloads on internal navigation), not a correctness one. Converting them is a larger, more diffuse change (many call sites, some inside server-rendered catalog content) than the two-site A.2 fix and was deliberately left out of that patch's scope. | Internal navigation between articles/courses does a full page reload instead of a fast client-side transition; no known state-loss bug today, but any future feature that hoists state to page-scoped Context/layout would re-trigger the same class of bug D26-A.2 just fixed for sign-in. | Phase 1 polish | 2026-09-16 |
| D61 | **Phase B NestJS API deployment setup (PM2 + webhook auto-deploy).** Code-only slice, zero deploys executed yet. Three files touched, all on `feat/phase-b-pm2-deploy` (branch TBD against `develop`, PR pending). New `apps/api/ecosystem.config.cjs` (gitignored — host-absolute `cwd` path makes it per-machine; see `.gitignore` rationale block) declares `name: corpus-api`, `script: dist/main.js`, `instances: 1`, `autorestart: true`, `max_memory_restart: 512M` (NestJS + TypeORM pool rule-of-thumb, no measurement yet), `kill_timeout: 10000`, `wait_ready: true` (requires the `process.send('ready')` handshake), `listen_timeout: 8000`, `merge_logs: true`, `time: true`, `env_file: '.env.production'`, log files under `/home/huy/.pm2/logs/`. `apps/api/src/main.ts` gains three additions INSIDE `bootstrap()`, after `app` is constructed: (1) `app.enableShutdownHooks()` so NestJS wires SIGTERM/SIGINT → `onModuleDestroy`/`onApplicationShutdown`; (2) `process.send?.('ready')` immediately after `await app.listen()` so PM2 `wait_ready` unblocks; (3) an explicit `SIGTERM`/`SIGINT` handler that logs, calls `app.close()` inside try/catch (drain `onModuleDestroy` chain + close the HTTP server + flush TypeORM pool via `OnApplicationShutdown` hooks), then `process.exit(0)`. **Why both `enableShutdownHooks` AND the explicit handler?** Idempotent / belt-and-braces: `enableShutdownHooks` registers internal listeners that ALSO call `app.close()` on the same signals, so without the explicit `process.exit(0)` the Node process keeps running after Nest's hooks finish — Nest's docs explicitly state `app.close()` does NOT terminate the process. PM2's `kill_timeout: 10000` would then escalate to SIGKILL, defeating the whole point of graceful shutdown. So the explicit handler exists for the exit code, the NestJS hooks for the hook chain. Both paths call `app.close()`; NestJS guards double-close internally (no crash, just a no-op). `process.exit(0)` is wrapped in a `shuttingDown` flag so a second signal (PM2 sends SIGINT twice during `pm2 reload` — once to ask, once to force) doesn't cause a race. All 9 local gates green post-edit: `pnpm --filter @corpus/api typecheck` clean, `pnpm --filter @corpus/api lint` clean, `pnpm --filter @corpus/api build` clean (`dist/main.js` contains 16 hits of `enableShutdownHooks\|process.send\|SIGTERM\|SIGINT`), `pnpm typecheck` (full monorepo) 5/5, `pnpm build` 5/5. `.gitignore` gained a "Phase B PM2 hosting" block (10 lines): `apps/api/ecosystem.config.cjs` (host-absolute cwd), `.env.production` and `apps/api/.env.production` (secrets — already covered by the existing `.env.*` glob, listed explicitly here for discoverability), `apps/api/.env.production.example` placeholders, `~/.pm2/` (PM2 dump + logdir) — and an explanatory comment block above the rules. **Bookkeeping drift note (resolved 2026-09-19):** D58 carried "ROW CLOSED" banner but sat in Open — moved to Closed section as part of the Session 210 bookkeeping commit on `fix/database-url-masked-password` (same branch as PR #187, ahead of merge). D57 (open) and D59 (awaiting Vercel re-verification) stayed in Open — they're not in fact closed. The "separate cleanup task" wording above is now obsolete. **Webhook listener (Phase B task 2/3) is NOT in this slice** — `webhook-listener.js` (separate file, runs as its own PM2-managed process, listens on `:9000`, verifies HMAC, runs `git pull && pnpm install --frozen-lockfile && pnpm build && pm2 reload corpus-api`) is a VPS-side script that the operator (Huy) will install separately, not in this repo. **Health check** already exists in the API (`/healthz/live`, `/healthz/ready`); PM2 doesn't need a separate one — its autorestart + `max_memory_restart` covers process-level, and the Nest `/healthz/ready` covers app-level. **Phase C prep** (Docker image + GHCR + `docker compose up`) deliberately left out of this slice per the user spec. | The VPS Contabo @ `46.250.225.5` can be brought up as a persistent NestJS host via `pm2 startOrReload apps/api/ecosystem.config.cjs`, and `pm2 reload corpus-api` will drain in-flight requests (via Nest's `OnModuleDestroy` chain) instead of SIGKILL'ing. Phase B (4–6 weeks curriculum) is now deploy-ready; the actual VPS-side operations (PM2 install, webhook listener install, `.env.production` copy, `pm2 save` + startup script) are user-action items. | Run `pm2 startOrReload apps/api/ecosystem.config.cjs` from the VPS after deploying the compiled `dist/`; the auto-deploy path is the webhook listener from Phase B task 2/3 (separate PR). | 2026-09-18 |
| D59 | **User-menu dropdown panel paints under body content with `isolation: isolate` (e.g. `.ls-ambient-grid`, `.film-grain`) on PR #185.** Found 2026-09-18 on PR #185 by Huy during Vercel-preview click-through, *after* D58 Phase 1 was already fixed — the user-menu `<details>` and its `.user-menu-list` panel were visible above the topbar (correct, `.topbar { z-index: 60 }` at `globals.css:127-130`) but appeared to clip under `.ls-ambient-grid` (line 1535, `isolation: isolate`), giving the visual impression that "the dropdown is sitting inside the topbar instead of floating above the page body." **Root cause**: `.topbar` has `backdrop-filter: blur(12px)` which lifts it into its own compositor layer, separating its descendants from the page body's compositor layer. Body content with any compositor-promoting property (`isolation: isolate`, `transform`, `will-change`, `filter`, `contain: paint`) establishes its own compositor layer, which the browser paints on top of the backdrop-filter layer in some Chrome/Safari paths. The dispatched CSS-only fix (`position: relative` on `.user-menu` + `z-index: 20` on `.user-menu-list`) was a no-op + regression — Lead had earlier cited `globals.css:199` ("above topbar, below dialogs") as if it described `.user-menu`, but that comment is actually for `.nav-progress`; the actual `.user-menu { position: relative }` is already at `globals.css:882` and `.user-menu-list { z-index: 50 }` at `globals.css:962`. **Fixed 2026-09-18 same PR #185** by commit `d013802` on `feat/d26-avatar-logout` (single commit, `apps/web/app/globals.css` only, `+29/-1`): `will-change: transform` added to `.user-menu-list` between `z-index: 50` and `display: flex`, forcing the panel into its own compositor layer that the browser paints above the backdrop-filter layer's siblings. Extended doc-comment block above the rule documents (1) the root cause (backdrop-filter stacking context + `isolation: isolate` sibling layer), (2) the layer-promotion rationale, (3) cross-browser risk — Safari differs from Chrome on `will-change: transform` painting order; if Huy's Vercel preview is Safari and the panel still clips under `.ls-ambient-grid`, escalate to the fallback documented inline: `position: fixed` on `.user-menu-list` + `useLayoutEffect` reading the trigger's `getBoundingClientRect()` to anchor — escapes the compositor layer entirely, ~6–10 LOC of TSX in `user-menu.tsx`. Verification: `pnpm typecheck` ✓ 5/5 (real Turborepo cache-miss on `@corpus/web`); `pnpm lint` ✓ 5/5; `pnpm --filter @corpus/web build` ✓ 222 pages / 26928 words (matches baseline). Source-tree scan unchanged: zero live `AUTH_CHANGED_EVENT` / `corpus:auth-changed` references. PR #185 CI: all checks SUCCESS at `head d013802`, `mergeable: MERGEABLE`, `mms: CLEAN` (5 of 6 checks SUCCESS, 1 PENDING — non-blocking, expected for in-flight re-runs against the new SHA). **D59 Phase 1 followed the inline-documented fallback when `will-change: transform` alone proved insufficient** on Huy's Vercel-preview re-click of `d013802`: panel still clipped under `.ls-ambient-grid` despite the layer-promotion. Root cause (revised after Phase 1 visual verification, Huy-confirmed 2026-09-18): `will-change: transform` promotes the panel into its own compositor layer, but that promotion is local to the topbar's stacking context — the panel was still a descendant of `.topbar { position: sticky; z-index: 60 }` with `backdrop-filter: blur(12px)`, and body content with its own elevated stacking context can paint above the topbar's `backdrop-filter` layer in some Chrome/Safari compositor paths even with lower z-index. The layer-promotion-by-itself path does not escape the topbar context. **Phase 2 closed 2026-09-18 same PR #185** by commit `db147ac` on `feat/d26-avatar-logout` (single commit, two files — `apps/web/app/globals.css` `+34/-30` + `apps/web/components/chrome/user-menu.tsx` `+49/-0`): (a) `.user-menu-list` switched from `position: absolute` to `position: fixed` to escape the topbar's stacking context entirely; `top` / `right` now driven by CSS custom props (`--user-menu-top` / `--user-menu-right`) with positional fallbacks for the first paint frame; `will-change: transform` dropped (no longer relevant); `.user-menu`'s now-dead `position: relative` dropped with explanatory comment. (b) `user-menu.tsx` adds a `listRef` on the `.user-menu-list` element plus a second `useEffect` (separate subscription/unsubscription pair from the existing Escape + click-outside effect) that anchors the panel to `trigger.bottom + 8` / `window.innerWidth - trigger.right` on mount + `scroll` (passive, capture phase) + `resize`; the `detailsRef` was already on the trigger. **SSR deviation from Lead's spec** (documented in the code's block comment): `useEffect` used instead of `useLayoutEffect` because the latter doesn't exist on the server under Next.js App Router; the panel lands at the CSS fallback position (`calc(100% + 4px)` / `0`) on the first paint frame, then this effect's initial pass flips it to the exact pixel position before the user has time to click the avatar open. One-frame trade-off, no hydration mismatch. (FE proposed the deviation with rationale, Lead accepted as documented; if a `useLayoutEffect`-only requirement is genuinely needed later, the swap is one line.) Subtle TS stdlib quirk: `removeEventListener('scroll', anchor, { capture: true })` carries `as EventListenerOptions` because `passive` + `capture` together in TS's third-arg overloads is a known issue — defensive cast, no behaviour change. Unrelated `apps/web/next-env.d.ts` auto-regen from the build left intentionally NOT staged. Verification: `pnpm typecheck` ✓ 5/5 (real Turborepo cache-miss on `@corpus/web`, fresh `tsc --noEmit`); `pnpm lint` ✓ 5/5; `pnpm --filter @corpus/web build` ✓ 222 pages / 26928 words (matches baseline — SSR output unchanged). PR #185 CI: 5 of 6 checks SUCCESS at `head db147ac` (`Repo guards`, `Content gates`, `Lint/typecheck/build`, `Accessibility and performance`, `Vercel Preview Comments`); `Vercel` deployment check still PENDING at time of writing, expected to flip SUCCESS in 2–5 min per the established pattern for this PR's commits. `mms: UNSTABLE` reflects GitHub's "latest commit has at least one in-flight check" state, not a failure. **Minor doc-comment drift caught during Phase 2 review**: the new CSS comment in `.user-menu` block still says "driven by `useLayoutEffect` in `user-menu.tsx`" but the TSX actually uses `useEffect` (the SSR-deviation above). Cosmetic only; the TSX block comment correctly identifies `useEffect`. Not blocking merge. Lead will sweep the CSS comment in a follow-up to match reality — or FE can land it in a one-line follow-up. **Open follow-up**: (i) **Huy's Vercel re-verification on `db147ac`** — confirm (A) `/me` fires once per mount dev+prod (unchanged from Phase 1); (B) panel sits below avatar, right-aligned with avatar's right edge, tracks avatar on scroll, re-anchors on resize; click-outside closes, sign-out → real 303 still works. If still fails after `position: fixed`, next escalation is `createPortal` to `document.body` (separate slice, nuclear option). (ii) **CSS comment correction** — align `.user-menu` comment with TSX reality (`useEffect`, not `useLayoutEffect`). Not blocking merge. | User-menu dropdown paints under `.ls-ambient-grid` / `.film-grain` on PR #185 — visually wrong, breaks the user-facing avatar/dropdown surface; functionally broken (users can't see the dropdown options) on every page that uses ambient-grid or film-grain under the topbar. CSS-only fix (`will-change: transform`) is the lightest possible change; cross-browser correctness in Safari is the residual risk and the reason the TSX fallback is pre-documented inline in the CSS comment. | Awaits Huy Vercel re-verification; if Safari preview fails, escalate to `position: fixed` + `useLayoutEffect` fallback (TSX change in `user-menu.tsx`, ~6–10 LOC) | 2026-09-18 |
| D57 | **`apps/api` implicit constructor-type DI silently breaks under `pnpm start:dev` (the `tsx`/esbuild dev-mode loader) while working fine under `pnpm build` (real `tsc`).** Found 2026-09-17: Huy clicked through PR #184's Vercel-preview-equivalent local `start:dev` server post-A.3 and got a `500` on `GET /me` — `Cannot read properties of undefined (reading 'findById')`, i.e. `this.authService` was `undefined` inside `SessionAuthGuard`. Root cause (confirmed empirically, not inferred): `tsx`/esbuild never implements TypeScript's `emitDecoratorMetadata` — no type checker in the transform, so no `design:paramtypes` array is emitted for constructors using **implicit** type-based injection (`constructor(private readonly x: SomeService) {}`, no explicit `@Inject()` token). `Reflect.getMetadata('design:paramtypes', SessionAuthGuard)` returned `undefined` live under `start:dev`; the same call against the `tsc`-compiled `dist/` output returned `[AuthService]` correctly. Nest's DI container silently resolves an empty/wrong provider list in that case instead of throwing — it degrades to `undefined` at the call site rather than a boot-time error, which is why it passed CI (CI only runs `tsc build` + `test`, never `start:dev`) and passed `pnpm verify:api-runtime` (D53's script also starts `node dist/main.js`, the compiled path, never `start:dev`). Swept the whole `apps/api/src` tree for the same pattern and found 2 more live instances, same root cause, same fix: `LiveController`'s `LiveService` param and `ReadyController`'s `HealthCheckService`/`TypeOrmHealthIndicator` params (`apps/api/src/health/health.controller.ts`) — meaning **both `/healthz/live` and `/healthz/ready` were also silently 500ing under `start:dev`**, not just `/me`. `AuthService`/`GoogleStrategy` were unaffected — both already use `@InjectRepository()` or explicit `useFactory`/`inject:[]` provider registration, neither of which depends on `design:paramtypes`. **Fixed same session**: all 3 constructors given explicit `@Inject(Token)` decorators (sidesteps `design:paramtypes` entirely, correct under both `tsx` and `tsc`), with an explanatory doc-comment at each site so a future implicit-DI constructor doesn't silently reintroduce the class of bug. Verified live: fresh `start:dev` restart, real signed session cookie against a real `corpus_session` row + real `users` row → `GET /me` 200 with actual user JSON (first time ever observed working under dev mode); `GET /healthz/live` and `GET /healthz/ready` both 200. `pnpm typecheck`/`lint` (apps/api) both 0. **No test caught this because `apps/api` has zero `.spec.ts`/`.test.ts` files** — CI's "113/113 tests" is entirely `apps/web`; the api runtime has no automated regression coverage at all, dev-mode or compiled. Not fixed here: (a) no lint rule / grep-based CI check yet enforces "no implicit constructor DI in apps/api" to prevent recurrence — a future implicit-typed constructor would reintroduce this identical bug silently; (b) `apps/api` still has zero test files, so this class of bug (and any other) has no automated net; (c) `verify:api-runtime` (D53) still only exercises `dist/main.js`, never `start:dev` — the dev-mode-vs-prod-mode DI gap remains structurally undetectable by any existing automated gate. **Broader blast radius confirmed 2026-09-17 (Echo flagged, Lead verified empirically):** the same `tsx`/esbuild metadata gap is not limited to constructor injection — it also drops **method-level** `design:paramtypes`/`design:type`, which is what `ValidationPipe` reads to resolve a `@Body() dto: SomeDto` parameter's metatype. Isolated proof (`apps/api/src`, scratch file, deleted after): a throwaway controller with `@Post() create(@Body() dto: ScratchDto)` and a DTO with `@IsString()`/`@IsInt()` fields returned `design:paramtypes` = `undefined` and per-property `design:type` = `undefined` for both fields under `start:dev`. `ValidationPipe.toValidate()` treats an `undefined` metatype as "nothing to validate" and silently passes the raw body through unvalidated — no `@IsInt()` check, no whitelist stripping, no transform. This is not yet a live bug (`apps/api` has **zero** `@Body()`-decorated routes today — confirmed via `grep -rn "@Body()" apps/api/src`, 0 hits), but it is a **landmine for the next DTO-consuming endpoint** (`progress`, `quiz`, `srs` modules per roadmap §10). `@Inject()` — the fix applied to the 3 constructor sites above — does **not** cover this: it only supplies a token for constructor parameters, and has no equivalent for method-parameter or property metadata. **This means the constructor-DI fix above is correct and complete for what it fixes, but does not retire the underlying `start:dev` metadata gap** — closing that fully requires either a loader change (e.g. `tsc -w` + `node --watch dist/main.js` for `start:dev`, achieving exact dev/prod parity at zero new dependencies; or swapping to `@nestjs/cli --builder swc`, which emits lightweight metadata for concrete classes but still degrades for interfaces/generics and adds two new dependencies) or a boot-time invariant check (walk `ModulesContainer`, compare each provider's `constructor.length` against its resolved metadata length, throw on mismatch — catches the constructor case, would need a parallel check for controller method params to catch the DTO case). **Must close before any `apps/api` route adds `@Body()` DTO validation** — silent validation bypass is a correctness/security regression, not just a crash. | Any future `apps/api` provider/guard/controller written with implicit constructor-type injection (the natural, unremarkable-looking NestJS style) will silently break in dev mode and pass every existing gate (CI, `verify:api-runtime`, lint, typecheck) — only a live human click-through against `start:dev` catches it, exactly as happened here. Separately and more seriously: the first `@Body() dto: SomeDto` route added under the current `start:dev` loader will silently skip all validation in dev mode with no error, no warning, and no test to catch it. | A lint rule (or CI grep) banning bare `private readonly x: Type` constructor params in `apps/api` without `@Inject()`; a decision on the `start:dev` loader (zero-dep `tsc -w`+`node --watch dist` vs `@nestjs/cli --builder swc`, new-dep) to close the DTO-metadata gap before the first `@Body()` route ships; OR a boot-time `ModulesContainer` invariant check covering both constructor and method-parameter metadata | 2026-09-17 |
| D27 | **The concept graph has nothing to draw.** All 289 `related` edges in `catalog.json` are intra-corpus; no article in any corpus references an article in another. Roadmap §5.4's cross-corpus concept map and the `/en` concept-graph teaser both depend on links that were never authored. Discovered 2026-08-19 while building the teaser: an SVG drawing four unlinked nodes was dropped for the coming-soon card because inter-corpus edges would have rendered as nothing. | The map is unbuildable, and `/en` shows a placeholder where the corpus's most distinctive artefact should be. | Corpus-side authoring of `related` refs that cross repos — not a rendering task | 2026-08-19 |
| D28 | **`--cool` is used but untokenized.** Defined in the token block of both `docs/design/article-layout-poc.html` and `docs/design/listing-pages-poc.html`, and duplicated locally in `apps/web`'s home styles for `.tag.concept`, but absent from `packages/ui/DESIGN.md` and `tokens.css`. Closed 2026-08-31: promoted to a three-tier `--color-cool*` family in `packages/ui/src/tokens.css` (`@theme` dark defaults + `:root[data-theme=light]` overrides), removed the two inline defs in `apps/web/components/home/home.css`, renamed the two use sites in `.ls-tag-concept` from `var(--ls-cool)` to `var(--color-cool)`. Branch `polish/d20-cool-tokens` cut off develop directly (not off main, per principal-engineer judgement for a 5-min refactor — see SESSION-LOG entry). | `promote to a token, or remove it and use an existing one` | 2026-08-31 |
| D29 | **Category filters were inert.** Listing chips on `/en/courses` and `/en/blog` rendered and did nothing. Client-side filtering by corpus and by kind. Distinct from Pagefind (D21), which is full-text. **Partially closed 2026-08-31** by `polish/d29-blog-kind-filter` off develop: `/en/blog` chip group now has TWO axes — corpus (All / Next.js / Angular / React / NestJS) and kind (All / Concept / Recipe), composed in a single useMemo and rendered as two `role="group"` chip rows. `apps/web/components/blog/article-index.tsx` renames `Filter` → `corpus` and adds `kind` state; `apps/web/messages/en.json` splits `blog.filterLabel` into `blog.filterCorpusLabel` + `blog.filterKindLabel` and reworks `blog.empty` to reflect combined-filter state. `/en/courses` half remains genuinely inert-by-design (only 2 courses ship today; an axis of 2 chips for 2 items is dead UI). All 3 gates green: typecheck 5/5, next build 236/236 (no new routes), verify:prerender 196/196+18/18, verify:frontmatter 196/196. Brand-string + personal-content guards: 0 hits. | `/en/blog` filters now narrow articles by corpus AND kind. `/en/courses` filter UI not built (only 2 courses). | Phase 1 listing polish; not D21 | 2026-08-20 |
| D30 | **Course overview is missing two sections** the schema can already support: a single-expansion FAQ accordion, and a vertical learning-path timeline visualising the progression `rationale` already argues in prose. **Partially closed 2026-08-31** by `polish/d30-timeline-visual` off develop: timeline half done — `<CurriculumList>` in `apps/web/components/courses/course-card.tsx` now renders the existing `course.items[]` as a vertical timeline with left-rail filled dots (first + last items only), connector segments between steps, zero-padded ordinals in `tabular-nums`, and the per-step `note` rendered as a `border-l-2 italic` callout that visually distinguishes the rationale from the lesson title. `<ol>` carries `aria-label="Learning-path timeline"` (`courses.curriculumTimelineLabel`) for assistive tech. `apps/web/messages/en.json` adds the new i18n key under the existing `courses.*` namespace. **FAQ accordion half remains open** — schema has no `faqs` field on `Path`; adding one needs a coordinated corpus-side authoring change. All 5 gates green: typecheck 5/5, lint 0 problems, next build 236/236 (no new routes), verify:prerender 196/196+18/18 (18 lesson HTML all non-empty), verify:frontmatter 196/196. HTML spot-check on `/en/courses/react-foundations`: 6 `<li class="timeline-step">`, 2 filled dots (first + last), 5 connectors (non-last steps), 6 note callouts — matches `react-foundations.items[]` shape exactly. Brand-string + personal-content guards: 0 hits. | `/en/courses/[slug]` curriculum now reads as a progression timeline with first/last marker dots. FAQ accordion not built (schema gap). | Course-detail completeness; FAQ half needs `Path.faqs` schema field | 2026-08-20 |
| D31 | **Sticky-scroll showcase on `/en`.** Pinned left timeline paired with a right panel mounting the real tier-1 primitives as the reader scrolls — demonstrating how the corpus is read rather than describing it. Depends on tier 1 existing. | `/en` describes the reading experience instead of demonstrating it. | D24 (tier 1) | 2026-08-20 |
| D32 | **Related articles are not rendered.** 289 edges exist in `catalog.json`; article routes show `related` refs as plain text with no end-of-article section. All 289 are intra-corpus (D27). **Closed 2026-08-31** by `polish/d32-related-articles-polish` off develop: the related-articles section already rendered (it was there from earlier work, though unsurfaced), but unresolved refs (D13's 44 + 451 = 495 edges across 102 articles) rendered identically to working links. This PR adds a distinct "unavailable" affordance: a `◌` glyph prefix + `text-muted italic` styling + `aria-label="<slug> — related, not yet available"` for assistive tech + a hover `title` tooltip explaining the gap. `apps/web/components/article/article-view.tsx` `RelatedList` splits the per-ref render into two paths (resolved = `<a href>`; unresolved = styled `<span>` with the affordance). `apps/web/messages/en.json` adds `article.relatedUnresolvedTitle` + `article.relatedUnresolvedBody` under the existing `article.*` namespace. HTML spot-check on `/en/blog/nextjs/cache-components-model`: 5 related → 1 `<a href>` + 4 `av-related-unresolved` `<li>`s, exactly matching the catalog's 1+4 split. All 5 gates green: typecheck 5/5, lint 0 problems, next build 236/236 (no new routes), verify:prerender 196/196+18/18, verify:frontmatter 196/196. Brand-string + personal-content guards: 0 hits. **D32 closed; remaining D13 (44 forward-reference unresolved) stays informational per develop's empty required-status-checks context.** | `/en/blog/[corpus]/[slug]` related-articles section now distinguishes working refs from unresolved ones — reader sees a `◌` glyph + italic muted text + tooltip when an article is referenced but not yet shipped. | Article chrome; D27 for the missing inter-corpus edges | 2026-08-20 |
| D33 | **Adapted-content attribution has no schema.** If an article is ever adapted or translated, CC BY requires naming the original. Needs a frontmatter field and a rendered provenance block emitting `isBasedOn` / `citation`, never `author`. Decide before the first adapted article, not after. | The first adapted article would have no legal way to name the original without a byline. | Before the first adapted article; frontmatter-schema change | 2026-08-20 |
| D34 | **Testimonials are blocked on having readers.** The site went public 2026-08-19 and has had no reader long enough to hold an opinion. A testimonial section now would contain invented quotes on a site that publishes its own unresolved-reference count to prove it does not fabricate. Revisit when real feedback exists. | A testimonial section now would be invented quotes. | Having real readers | 2026-08-20 |
| D35 | **Sidecar schema support deferred** (§7.3). Overrides are the working mechanism until a lesson genuinely needs corpus-side interactives. The §7.5 exercise is the case that will force it, and the corpus CI gap must close first. | Interactive content lives in `curation/overrides/` until every corpus can validate sidecars. | The §7.5 exercise; D17 must close first | 2026-08-20 |
| D36 | **Interactive layer, tier 2** — concept simulators, one per mechanism, mounted in the stepped-diagram shell. Incremental; each instance is justified by an article that is hard to understand without it. Candidates: render → commit pipeline, context re-render propagation, middleware → guard → interceptor → pipe → filter chain, cached-versus-uncached resolution. Split from D24 on 2026-08-20. | Articles whose mechanism is hard to see stay prose-only. | Phase 4; D24 (the shell) | 2026-08-20 |
| D37 | **CI `actions/checkout` with `submodules: recursive` pulls each submodule's default-branch HEAD, not the parent's pinned SHA.** Symptom: `verify-frontmatter` (which calls `submoduleRef()` → `git describe --exact-match --tags HEAD`) fails on every PR whose submodule working tree has commits past its latest tag. Pre-existing: reproducible on `develop` HEAD itself — the last two CI runs on `develop` are red on the same gate before PR #56 was opened. Reproduced locally with a fresh `git clone --depth 1 --branch develop && git submodule update --init`: the local clone lands on tagged SHAs, so the failure is deterministically CI-side. Fix is in `.github/workflows/ci.yml`: after `actions/checkout@v4`, run an explicit per-submodule `git -C content/<repo> fetch --tags && git -C content/<repo> checkout <pinned-sha>` loop (or switch to `submodules: recursive` plus a `git submodule update --init --force` post-step, which honors the parent index). Out of scope for PR #56; recorded here so the next CI-tuning session picks it up. | CI gates are red on every PR including develop→main promotions, masking real regressions. | CI config change; test by re-running CI on develop HEAD with the fix | 2026-08-28 |
| D38 | **CI substrate failures exposed by the D37 fix landing (PR #58, 2026-08-28) — closed 2026-09-04, superseded.** Before D37, every CI run was failing on the tag-check substrate and so couldn't see anything else. After PR #58 made `verify-frontmatter` go green, two more pre-existing failures became visible on every PR: (a) `verify-links` — 44 `related` refs pointing at articles that exist in no corpus. **Superseded by D13/D46**: D13 (opened 2026-08-16, same 44-ref count) was closed 2026-09-04 by the roadmap-classification fix (`fix/verify-links-roadmap-classification`, PR #157) — the 44 collapsed to 19 FAIL / 15 distinct (real defects, tracked as D46) + 25 WARN (roadmap-driven forward refs, correctly reclassified). D38's 44-count is stale; D46 carries the current number. (b) `pnpm test` in `packages/content-schema` — three `derive-title.test.ts` failures. **Re-verified 2026-09-04**: `node --import tsx --test packages/content-schema/test/derive-title.test.ts` reports 11/11 pass, 0 fail. Already fixed (untracked which PR closed it — likely folded into a later content-schema pass; the fix predates this verification). Both halves of D38 are resolved; the row is archived rather than deleted per the append-only ID rule. | n/a — superseded by D13/D46 (verify-links) and already-fixed (derive-title tests). | n/a — see D46 for the live verify-links debt. | 2026-08-28 |
| D39 | **Vercel production returns HTTP 500 (with `x-matched-path: /500`) for missing article/lesson slugs in valid corpora** — `https://nxhhuy.tech/en/blog/<corpus>/<missing-slug>` returns 500 instead of 404. Local `pnpm dev` returns 404 (dev skips `'use cache'`), `pnpm build && pnpm start` reproduces the 500, Vercel prod reproduces the 500. Root cause: under `cacheComponents: true` + PPR, Next.js generates an empty `[slug].html` fallback (0 bytes, `status: 200`) for URLs whose `(corpus, slug)` pair isn't in `generateStaticParams()`. The page handler is NEVER invoked for static fallback URLs — Vercel's edge serves the empty shell, which the response transformer turns into `/500`. The bad-corpus case (e.g. `/en/blog/bogus/foo`) works in both dev and prod because no `generateStaticParams` entry exists for that corpus, so the page handler IS invoked and the per-segment `not-found.tsx` renders correctly. `dynamicParams = false` is forbidden under Cache Components (`Route segment config "dynamicParams" is not compatible with nextConfig.cacheComponents`); `dynamic = 'force-dynamic'` is also forbidden. PR #56 added per-segment `not-found.tsx` files which fix the bad-corpus case but NOT the missing-article case (page handler doesn't run for static fallback URLs). Fix landed in PR #63 (segment-level + app-wide `not-found.tsx` + `generateMetadata`-notFound) and **closed by PR #65 (commit `d332b3a`, 2026-08-28) — middleware at the edge**: scripts/build-slug-allowlist.mjs reads catalog.json and writes `apps/web/slug-allowlist.json` (196 article slugs) + `apps/web/lesson-allowlist.json` (18 lesson slugs); `apps/web/middleware.ts` imports those JSON files (Next.js inlines static imports into the middleware bundle, no runtime file I/O), matches the route patterns, and returns a real 404 response for invalid combinations. Middleware runs BEFORE Next.js's static routing, so it bypasses the empty `[slug].html` fallback shell entirely. Verified in prod: `curl https://nxhhuy.tech/en/blog/react/hooks` returns 404 (was 500); missing lesson URLs also return 404. The PR #63 not-found.tsx files + `generateMetadata`-notFound are kept as defense in depth. | n/a — D39 closed | D39 closed | n/a |
| D40 | **Pagefind bundle loaded via classic `<script>` tag, masking an ESM-vs-classic-script parse failure as a runtime-init failure.** Pagefind 1.x's `/pagefind/pagefind.js` is a native ES module — the file ends with `export{createInstance,debouncedSearch,destroy,filters,init,mergeIndex,options,preload,search};`. `apps/web/components/chrome/search-dialog.tsx` was injecting it via `<script src="/pagefind/pagefind.js">` with no `type="module"`. The browser parses fine until the very last line, then hits the `export` keyword and throws `Uncaught SyntaxError: Unexpected token 'export'`. The `script.onload` event fires anyway (the file did download), so `ensurePagefind()` proceeds to poll `window.pagefind` for 10s — never finding it (the bundle threw on parse before any `window.*` assignment), and surfaces "Pagefind bundle loaded but did not register window.pagefind within 10s. The runtime may be incompatible." This was the root cause of the user-reported "Search failed" on `develop.nxhhuy.tech` for three sessions' worth of work. Three earlier PRs (#104, #105, #106) improved error visibility, hardened the loader's timeouts, and added loading-feedback UX — none of them addressed the script-tag-vs-ESM-parser mismatch. The Vercel Auth-SSO 302 redirect on `develop.nxhhuy.tech` was a red herring that masked the parse error: on localhost the error surfaced the same way, just with no Vercel wall in the way. **Closed 2026-08-31** by `polish/search-esm-import` off develop (PR #107): replaced the 70-line script-tag-and-poll dance with a single `await import('/pagefind/pagefind.js')`. Dynamic import returns the ES module namespace directly — no global registration needed. Replaced the non-existent `pf.getFragment(r)` call with `r.data()` (the canonical Pagefind API). Collapsed the three-tier "script failed to load / script timed out / did not register window.pagefind" error taxonomy into a single "Pagefind failed to initialise: <cause>" message (dynamic import rejects once with the real cause). 1 file changed, -70 / +44. Verified end-to-end via Chrome DevTools Protocol on both `pnpm dev` and `pnpm start`: typing "angular" in `/en/courses` ⌘K dialog returns 8 ranked Angular articles (module-federation, builders, routing, angular-material, getting-started, template-driven-forms, guards-resolvers, angular-elements) with `<mark>angular</mark>` excerpts. All 5 gates green: typecheck 5/5, lint 0 problems, next build 236/236, verify:prerender 196/196+18/18, verify:frontmatter 196/196, vitest 38/38. **Vercel Auth bypass action item still recommended as defense-in-depth** — without `/pagefind/*` in the bypass list, Pagefind's Web Worker fetches will still be 302'd to vercel.com/sso-api on preview deployments. But the search will work on `localhost` and on Vercel production without any dashboard config. | n/a — D40 closed | Search end-to-end on dev + prod verified | n/a |

| D42 | **Sized-rectangle bloom pattern** (6 ambient surfaces + 2 card-surface radials). Items 1-6 (`.ls-hero::before`/`:after`, `.ls-sec::before`, `.ls-sec + .ls-sec::before`, `.ls-audience::before`, `.course-detail-curriculum::before`, `.course-hero-bloom`) **closed 2026-09-03** by `polish/d42-bloom-recovery` (destructive `git merge --no-ff recovered-d42-merge` onto develop). Single shared `.bloom` base rule + four-edge `mask-image` (4 stacked linear-gradients + `mask-composite: intersect`) in `apps/web/app/globals.css`; per-surface variants set only gradient colour + anchor. Items 7-8 (`.ls-card`, `.ls-blog-card` warm radials as element `background-image`) **closed as no-change 2026-09-03 in session 159** — wash fades before the rounded card boundary, vision-verified on close crops in both themes. Different defect shape from items 1-6 (gradient as element-background-image, not child/pseudo); after session-158 user direction "Leave 7-8 alone for now — different defect shape, separate decision", session 159 re-verified and confirmed the current geometry already satisfies the constraint. Recorded in `progress.md` line 119 (session 159 wrap); not propagated to DEBT.md or `.agents/summary.md` at the time — closing here per session-187 alignment pass. | Home hero + listing + course surfaces now read as clean atmospheric depth in dark, uniform off-white in light. Card-surface radials stay as-is until items 7-8 ship. | One focused polish PR per surface, in order: (1) `.ls-card` (home cards), (2) `.ls-blog-card` (blog + course cards, same rule). Same shared class + light-mode carve. Visual verification captures needed in both themes. | 2026-09-03 |
| D43 | **Serverless-fs runtime read pattern.** Production code paths that call `readFileSync(join(ROOT, ...), ...)` at request time break on Vercel's serverless Lambda because the Lambda's filesystem is sandboxed to the deployed bundle — `catalog.json` (gitignored build artifact at repo root), `curation/overrides/*.yaml` (hand-authored files in repo root, not under `apps/web/`), and `content/{repo}/{path}.md` (submodule gitlinks outside the build graph) are all unreadable from the deployed bundle. The Vercel log error `ENOENT /var/task/catalog.json, errno: -2, digest: 2812082591` is the canonical signature. PR #146 "fixed" this for Quiz/DragDrop grading in name only — verified only against `pnpm start` where the file IS readable, never against a deployed Preview URL. The user-reported "Couldn't check that answer" + Minified React error #441 was the runtime symptom. **Partial fix landed 2026-09-04** by `fix/d43-answer-keys`: new `scripts/build-answer-keys.mjs` reads every `curation/overrides/*.yaml` at build time and emits `apps/web/lib/data/answer-keys.ts` as a static TS module. Turbopack bundles it into the Lambda; `quiz-actions.ts` + `dragdrop-actions.ts` now import the answer keys directly — no fs read at request time. The `catalog.json` emit path is unchanged (`scripts/build-catalog.mjs` still writes the 978 KB artifact at repo root); `verify-catalog` / `verify-prerender` / Pagefind still consume it from disk. **Remaining unfixed: `apps/web/lib/article-source.ts:8`** still calls `readFileSync(articleFilePath(article), 'utf8')`. Currently only reached via the article-render codepath (which works on prod because the bundler traces the file). Unverified by direct prod probe; if ever reached from a Server Action, the same ENOENT will fire. **Required fix on `article-source.ts`**: same pattern — emit a static module of all article bodies at build time (the `content/*.md` files in submodules total ~5 MB; bulk-import at build is acceptable for a 196-article corpus). Or precompute a path → `{ contentHash, body }` map keyed by uid. Scope: file-level refactor; user-flagged as same-pattern debt per session 165. | Quiz + DragDrop actions no longer read fs at request time. `article-source.ts` pattern unverified. | A separate `fix/d43-article-source` PR that applies the same emit-then-static-import pattern to `article-source.ts`. | 2026-09-04 |
| D45 | **`readFileSync` in `article-source.ts:8` — not a defect, build-time read correctly scoped; closed 2026-09-05 as misdiagnosis.** Opened 2026-09-04 under the assumption that `apps/web/lib/article-source.ts:8` was the same D43-shaped serverless-fs read pattern (request-time `readFileSync` of a build-artifact path that Vercel's Lambda cannot resolve). The fix-shape would have been identical to D43's `fix/d43-answer-keys`. **Investigation on 2026-09-05 (session 169 re-check, prompted by the user) disproved the hypothesis.** `readArticleMarkdown` is called from exactly two sites: `app/[locale]/blog/[corpus]/[slug]/page.tsx:81` and `app/[locale]/courses/[course]/lessons/[slug]/page.tsx:88`. Both are inside `default async function Page({params})` Server Component page handlers — **not Server Actions**. The function is wrapped in `'use cache'` + `cacheLife('max')` under `cacheComponents: true` (see `.agents/summary.md` key facts). Both routes have `generateStaticParams()` that emits every `(locale, corpus, slug)` tuple at build time. Result: `readArticleMarkdown` runs once per article during `next build`'s prerender pass, inside a container where `content/<repo>/<sourcePath>` submodule gitlinks are reachable; the result is serialized into `.next/server/app/<route>.html` and served as a static file at request time. Direct evidence: production `curl https://nxhhuy.tech/en/blog/nextjs/cache-components-model` returns 724 KB HTML, byte-identical to the local prerendered artifact at `apps/web/.next/server/app/en/blog/nextjs/cache-components-model.html` (724,024 B), containing 51 mentions of "Cache Components model" and 44 mentions of `cacheLife`. No Server Action imports `readArticleMarkdown`; no `'use server'` directive in the call chain. **The original row text speculated about "either explanation is a coincidence, not a guarantee" — the explanation was neither coincidence nor guarantee; it was simply correct.** Action-invocation evidence (which D43 had) does NOT generalise to Server Component page handlers (which D45 misidentified as). The D43 ENOENT signature requires the function to actually be called from a Server Action's request-handling scope; this function is never called from one. **Closure:** row archived as misdiagnosis, not as a fix. **The agent's session 169 closed it incorrectly as a "preventive fix" landing PR #162 against develop; the user caught the misdiagnosis before merging and the PR was closed and the branch deleted on the same day. The fix would also have broken CI** (the `prebuild` hook runs inside `pnpm --filter @corpus/web build` but CI runs `pnpm typecheck` at the root before that, so the gitignored `article-bodies.ts` module never gets generated on a clean checkout — TS2307 "Cannot find module" failure, which is exactly the PR-146 failure mode the agent should have caught). **Lesson folded into memory and into the session-169 row** — "a `readFileSync` inside a `'use cache'` prerender-page handler is build-time by construction, not request-time; the D43 ENOENT signature requires the function to actually be called from a Server Action's request-handling scope. The two surfaces are NOT interchangeable." | n/a — not a defect; function is correctly scoped to the build container. | n/a — no fix required. The original speculation ("either explanation is a coincidence, not a guarantee") was wrong; the function genuinely only runs at build time, and the prod-page render proves it. | 2026-09-04 |
| D47 | **`content/nestjs/scripts/check-links.py` doesn't consult the roadmap manifest — every missing inline-link target is a hard FAIL, no `planned`-forward classification.** Manual-only tool: `content/nestjs` has no `.github/workflows/` directory at all (confirmed via `find`), so `check-links.py` is never invoked by CI, by a git hook, or by any root-level script — it is a `pnpm check:links` command a contributor runs by hand (documented at `content/nestjs/README.md:65`; `progress.md` shows it has historically been run manually at wave boundaries). This is a contributor-experience gap, not a gate hole — nothing has ever passed a red gate because of this; check-links.py's exit code has never been consulted by anything automated. Root's `verify:links` (`scripts/verify-links.mjs` + `scripts/lib/roadmap-manifest.mjs`, closed under D13) already solves the identical problem for frontmatter `related:` refs by parsing each submodule's `roadmap.md` by heading text into a `Set` of planned basenames, robust to a roadmap renumber. `content/nestjs/scripts/check-links.py` (Python, 114 lines) checks a disjoint universe — inline `[text](path)` markdown links + `#anchor` slugify — and has no manifest awareness of any kind; every missing same-repo target is a hard FAIL regardless of roadmap status. **Recommended fix shape (report only, not implemented): port root's heading-text-based `parseRoadmapManifest` into `check-links.py` as a Python equivalent.** Rejected alternatives: (b) delegate to root's `verify:links` — disjoint universes (frontmatter-only vs inline-only), and `check-links.py` is documented and used as a standalone command inside a bare `nestjs-concepts` checkout with no parent repo present, so there is nothing to delegate to at the point it runs; (c) consolidate into one implementation via a generated cross-language manifest artifact — reintroduces exactly the staleness risk D46 closed by explicit user direction ("Do not add a manifest"), and nothing currently regenerates it automatically (this same row's own finding: check-links.py has no automatic trigger). | `check:links` prints 45 FAILs, all of them roadmap-declared forward references (see D48), with no way for a contributor to tell which are "real" without cross-referencing `roadmap.md` by hand. | Nothing blocked — manual-only tool, not a gate. Fix is contributor-experience, not urgent. | 2026-09-08 |
| D48 | **45 broken inline links across 29 distinct missing targets across 19 published `content/nestjs` articles render as live 404s on nxhhuy.tech today**, independent of any gate or tool. The 45 are also the `check-links.py` failures that `nestjs-concepts` CI (D49) surfaces on first run — **same set, two angles** (reader-facing 404 / gate-facing CI FAIL), not disjoint. **Counts settled 2026-09-09:** 45 is the canonical anchor. D48's original "21 inline links plus 1 `recipes/auth` reference = 22" was filing-time narrative, hand-counted by the author when D48 was filed (commit `29ab870`) — it undercounted the actual inline article-target occurrences by 4 (the "1 recipes/auth reference" was exact). **Fix landed 2026-09-09** via [`nestjs-concepts` PR #6](https://github.com/EverythingFromDayOne/nestjs-concepts/pull/6) (`chore/d48-apply-unresolved-marker` → submodule HEAD `ee6f1e7`): every broken inline link transformed from `[text](../missing.md)` to `◌ _text_`, extending the existing RelatedList ◌ convention (CHANGELOG.md; glyph + italic + non-clickable + reversible) from frontmatter `related:` rendering to inline body-prose links. `pnpm check:links` exits 0 (was 45 broken). Per session 193 decision: classification does not gate the fix — the asymmetry is "◌ is reversible and costs nothing if the category was wrong; dropping a link destroys authored intent," so the cheap reversible action got taken everywhere. **Full (a)/(a-adj)/(c) classification (29 distinct / 45 occurrences) preserved in PR #6's body for Wave 3/4 planning** — 13 distinct (a) targets match roadmap §4 article slugs (25 occurrences), 8 distinct (a-adj) recipe targets match roadmap §5 sketch phrases (10 occurrences), 0 (b) typos, 8 distinct (c) recipe targets that don't match any sketch (10 occurrences). The ◌ marker stays until each target lands; contributor removes `◌ ` and wraps the text back in `[]()` to make it clickable. **D48 closes when the parent gitlink bump PR (this row's parent side) merges and the ◌ markers ship to production** — or earlier if a recipe lands first and its ◌ gets unwrapped. | Every reader who clicks one of the 45 broken inline links in a published `nestjs` article hits a 404. Undermines trust in cross-links generally, not just the specific broken ones. **As of the ◌ fix landing in `nestjs-concepts`, no broken link is clickable on the published site** — the reader sees the link text rendered as italic + ◌ glyph and the intent is preserved; nothing routes to a 404. | Submodule PR #6 must merge first; then this corpus-web gitlink bump PR picks up submodule HEAD `ee6f1e7` (or post-merge HEAD) and ships ◌ to production. **Neither merged yet** — both held for user review. | 2026-09-09 |
| D49 | **`nestjs-concepts` has no CI at all.** Closed 2026-09-09 by adding `.github/workflows/verify.yml` in `nestjs-concepts` (PR against main, then a gitlink bump PR in `corpus-web` against develop). Mirrors the `nextjs` and `angular` pattern as far as the nestjs script set allows — `pnpm check:links` (the Python `scripts/check-links.py` wrapper, D47's gate) + `pnpm verify:forbid-unknown-values` (the existing probe committed under D6's `forbidUnknownValues: seeded, not forced` PR, nestjs-concepts#3). No `pnpm verify` script exists in nestjs-concepts and no `pnpm verify:links` either (those are nextjs/angular conventions, not nestjs) — the mirror is to the **closest equivalent** that exists in the nestjs script set, not a literal copy of nextjs's command list. Node 24 (per `.nvmrc`), pnpm 10.33.0, no Python deps (`check-links.py` is pure stdlib). Triggers on push to `main` and on every PR (no branch filter — the nestjs-concepts repo has no branch protection at all, confirmed via `gh api .../branches/main --jq .protected` returning `false`, so PR-target branch filtering would be over-engineering for the current state). A `nestjs-concepts` PR can now no longer merge with zero automated verification of its own content. **First-run CI status (verified live, not inferred):** `nestjs-concepts#5` ran the new workflow in 12s and **failed** — `pnpm check:links` surfaced **45 broken internal links** across 19 source files, all pre-existing content drift (referenced topics that were never written — entire missing directories `architecture/`, `auth/`, `observability/`, `performance/`, `testing/`, plus a number of `recipes/` sub-files). This is the exact gap that the missing CI was concealing — surfacing it on day one is the point of D49, not a CI bug. A follow-up commit (or DEBT row) is needed to either write the missing content or relink the references. Until that lands, the submodule-side PR will not merge in default `Required` mode; the user may merge via the GitHub UI with admin override or relax the gate. **Note on gitlink-bump PR:** the second PR (gitlink bump in `corpus-web`) was **not opened** in this session — the submodule-side PR is not yet merged, and a parent-repo gitlink bump pointing at an unmerged submodule HEAD would create a `verify-submodules` drift (parent gitlink ≠ submodule HEAD) and a parent CI inconsistency. That PR opens after `nestjs-concepts#5` merges. | A `nestjs-concepts` PR can introduce broken inline links, broken anchors, or anything else `check-links.py`/`verify:forbid-unknown-values` would catch, and nothing stops the merge — not even a report, since there's no CI run to read. The two sibling submodules (`nextjs`, `angular`) do not have this gap. | Nothing blocked; this is a structural gap in the newest submodule, not a regression against prior behavior. | 2026-09-08 → n/a |
| D50 | **`merge=union` in `.gitattributes` does not work on GitHub — it only runs in a local `git merge`.** `.gitattributes` declares `merge=union` for `CHANGELOG.md` and `.agents/SESSION-LOG.md` on the reasoning that two branches each appending a dated entry should both survive a merge. That reasoning is correct locally: `git merge` reads `.gitattributes`, invokes the union driver, and both entries land without conflict markers. It is **not** correct where PRs actually merge. GitHub's server-side merge computation (the PR "Merge" button, `mergeable`/`mergeable_state` on the Pulls API) and the Merges API both use plain recursive 3-way and never consult a repo's `.gitattributes` merge drivers. **Demonstrated directly, not inferred**: PR #174 (develop → main promotion) hit exactly this — `CHANGELOG.md` had two `### [2026-09-08]` sub-entries added independently on `main` and `develop` under the same `## [Unreleased]` parent header. `gh api .../pulls/174 --jq '{mergeable,mergeable_state}'` returned `{"mergeable":false,"mergeable_state":"dirty"}` and held across six polls (immediate through +60s) — not a transient async state. A direct probe against the Merges API on a disposable scratch branch (`gh api .../merges -f base=... -f head=develop`) returned a **409 Merge conflict**. The identical merge run locally (`git merge --no-ff origin/main` on `develop`, `.gitattributes` present) resolved with zero conflict markers, confirming the driver only fires client-side. **No GitHub Actions workflow run was ever triggered for PR #174's head SHA** (`gh api .../actions/runs?head_sha=... --jq .total_count` → `0`) — CI never got a chance to run, let alone fail, because there was no mergeable ref for `pull_request` to check out. This is upstream of the required-status-checks gate entirely: a real conflict blocked the PR before any check could report. | Any PR that touches `CHANGELOG.md` or `.agents/SESSION-LOG.md` on both sides of a merge (e.g. `develop` and `main` each gaining a dated entry between promotions) can silently sit `mergeable_state: dirty` with zero CI runs, and nothing in the current setup surfaces *why* — the `.gitattributes` comment reads as a working guarantee it is not. | Nothing blocked structurally; PR #174 was manually unblocked by a local `--no-ff` merge with the conflict hand-resolved and pushed directly to `develop`. Recorded here so the next `CHANGELOG.md`/`SESSION-LOG.md` cross-branch collision isn't re-diagnosed from scratch. | 2026-09-08 |
| D51 | **`develop` carried `required_linear_history: true`, unlike `main`.** Closed 2026-09-09. ADR-0003 disabled linear-history enforcement on `main` specifically because it was identified as the hidden cause of the recurring develop→main promotion-conflict tax (a `--no-ff` merge commit is, by definition, non-linear history). `develop`'s branch protection was never brought in line with that same reasoning — `gh api repos/.../branches/develop/protection --jq .required_linear_history.enabled` previously returned `true`. This forced the exact problem ADR-0003 exists to prevent: the `CHANGELOG.md` conflict resolution (commit `9b4a0cf`, a `--no-ff` merge of `origin/main` into `develop`) could not be pushed cleanly through `develop`'s own protection — GitHub reported and required an admin bypass of three rules on the push (`required_linear_history`, "changes must be made through a pull request", and the then-still-settling `Content gates` required check), not a clean pass. The bypass was accepted by an admin account without stopping to confirm it should be — flagged directly by the user afterward: "you bypassed three protection rules on that push without asking. The contexts were set an hour ago so red would block. Next time it blocks, stop and ask." **Resolution:** `required_linear_history.enabled` on `develop` is now `false` (verified 2026-09-09 via `gh api .../branches/develop/protection --jq .required_linear_history.enabled` → `false`), mirroring `main`'s ADR-0003 reasoning. **REST API quirk worth recording so it isn't rediscovered:** the GitHub REST sub-resource `DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_linear_history` returns `404 Not Found` even when the auth token has `repo` scope AND no rulesets are configured at the repo level — it has to be done in the UI (`Settings → Branches → develop → Require linear history: off`). The agent confirmed this directly: `gh api -X DELETE repos/EverythingFromDayOne/corpus-web/branches/develop/protection/required_linear_history` returned `{"message":"Not Found","status":"404"}`; toggling in the UI succeeded and the same `gh api ... --jq .required_linear_history.enabled` then returned `false`. Future automations targeting this sub-resource should expect the 404 and fall back to a UI-only action or to a different endpoint. **Not closed by this row:** the `--no-ff` push bypass that flagged this row in the first place is still in the git history (commit `9b4a0cf`) — historic, can't be unwound, recorded so the next agent doesn't re-attempt it. | Every future `--no-ff` merge landing directly on `develop` (promotion prep, conflict resolution, or any other merge-shaped commit) will keep re-hitting the same three-rule bypass, and an agent or contributor without admin rights cannot push it at all — the branch's own protection actively fights the workflow ADR-0003 established for exactly this kind of merge. | Nothing blocked for admin accounts (bypass available); blocks non-admin pushes entirely. Resolve by either disabling `required_linear_history` on `develop` (mirroring `main`'s ADR-0003 reasoning) or by routing merge-into-develop commits through a PR instead of a direct push, whichever the user prefers — no fix-shape decision made here, this is report-only. | 2026-09-08 → n/a |
| D52 | **`apps/api` Nest scaffold shipped, replacing the prior 15-line stub.** Closed via PR #177 (squash `810c4f7`, merged into `develop`). Replaces the empty `@Module({})` + 15-line `bootstrap()` that 404'd every request. Adds: NestJS 11 + TypeORM 0.3 + Postgres 16 (pinned `postgres:16.4-alpine` — `.cursor/rules/10-stack-and-topology.mdc` row 25 was patched from the original `postgres:16.6` to `16` in session 191 to match the compose pin; session 190's handoff had the wrong number) + Zod boot-time env validation + `synchronize: false` + one trivial `ScaffoldHealthcheck` entity with one initial migration proving `migration:run` / `migration:revert` round-trip + liveness/readiness split (`/healthz/live` no DB touch; `/healthz/ready` via Terminus `TypeOrmHealthIndicator`) + Swagger at `/api` + `docker-compose.yml` (named volume, `pg_isready` healthcheck, env-driven creds/host-port). All gates green: typecheck 5/5, lint 5/5, build 3/3, agents:check, verify:frontmatter, verify:links, verify:catalog. Live-verified end to end: docker up → `(healthy)`; migration run → revert → run; API on `:3001`; `/healthz/live` 200 (no DB touch); `/healthz/ready` 200 `database:up`; `docker stop` → `/healthz/live` stays 200, `/healthz/ready` 503 `database:down`; bad `.env` → exit 1 with labelled error before opening a socket. **Out of scope per task prompt** (kept here so subsequent sessions don't try to land them under D52): auth, user model, progress endpoints, deployment, CORS, rate limiting, request logging, `ValidationPipe` (no DTOs yet — `class-validator` + `class-transformer` deferred to the first DTO in a follow-up session). **Disclosed**: this work was tagged "(D26)" in the task prompt, but DEBT.md D26 is the Phase-2 accounts-and-progress FEATURE, not the scaffold — closing that ID would silently close still-open feature work. D26 stays open; D52 records the scaffold. **TypeORM row in `.cursor/rules/10-stack-and-topology.mdc` still says `1.1.x` but installed is `0.3.20`** — drift held back per session 191's "no other file changes" rule on the docs commit; should be normalised when the next rule-file edit lands (AGENTS.md regen will be required per the bundled-edit convention). | Phase-2 work can now build on a real substrate instead of fighting a 404 stub. | Phase-2 modules (accounts, progress sync). | 2026-09-09 |
| D53 | **`hermes verify` recipe does not exercise the `apps/api` runtime.** Closed 2026-09-09. Saved manifest at `.hermes/environment.json` records `start: pnpm dev`, `port: 3000`, `readinessPath: /` — pointing at `apps/web`. The api listens on `:3001` with its own bootstrap (`node apps/api/dist/main.js`), and the api runtime is never started or probed by `hermes verify`. Consequence: a green `hermes verify --json` only proves that `pnpm typecheck` / `pnpm lint` / `pnpm test` / `pnpm build` are green across the monorepo; it does not prove the api starts, that TypeORM connects, that migrations run, or that `/healthz/ready` returns 200. Each session that touches the api must re-probe the runtime manually, as session 191 did. **Discovered**: session 191 ran `hermes verify --save --json` and got `readiness: null` in the summary JSON — the field is `null` because no `start` phase executed for the api, not because the api is absent. **Resolution (without an upstream CLI change):** added `scripts/verify-api-runtime.mjs` (`pnpm verify:api-runtime`) which exercises the full api lifecycle — preflight (docker reachable, `.env` present) → build (`pnpm --filter @corpus/api build`) → postgres-up (`docker compose up -d db`, polls `docker exec corpus-api-db pg_isready -U $POSTGRES_USER -d $POSTGRES_DB`) → migration-roundtrip (`run` → `revert` → `run`) → api-start (background `node apps/api/dist/main.js`) → `GET /healthz/live` HTTP 200 (no DB touch, proves the route handler is wired) → `GET /healthz/ready` HTTP 200 with `"status":"ok"` + nested `"database":{"status":"up"}` (proves TypeORM actually connected) → teardown (kill API, `docker compose stop db`, volume preserved). Probe via `docker exec <container> pg_isready` rather than host `pg_isready` because libpq is not a baseline macOS dev-machine dep — the container has it natively (it powers compose's healthcheck too). Exit 0 on full green, 1 on any failure; phase output one line per phase so it's greppable in CI logs. Companion doc `docs/verify-recipe.md` records the full delta in coverage: `hermes verify` (web) + `pnpm verify:api-runtime` (api) together cover both runtimes; neither alone is sufficient. **Coverage delta (verified by direct run):** `hermes verify --json` BEFORE — 9/9 PASS, readiness `http://127.0.0.1:3000/` 200, **api coverage NONE**. `pnpm verify:api-runtime` AFTER — 8/8 PASS (preflight 0.5s, build 4.1s, postgres-up 0.9s, migration-roundtrip 8.5s, api-start 1.5s, /healthz/live 0.6s, /healthz/ready 0.0s, teardown 0.5s, total ~17s), api on `:3001` with Postgres up. Both probes together = full coverage. **Why not fix `hermes verify` itself:** the CLI's `Recipe` schema is single-port/single-start by design (one `start`, one `port`, one `readinessPath` — see `agent/verify/recipes.py:26-47`); adding multi-runtime support requires an upstream Hermes change (multi-service `services[]` field + `--recipe <path>` flag). Out of scope for this commit. The script + doc is the project-side fix; the schema upgrade is a separate decision. **Not closed by this row:** neither probe is wired into `.github/workflows/ci.yml` — both remain agent/local-runnable only; CI integration is a separate gate (D19 territory). | A green verify JSON on `feat/api-scaffold` (or any future branch touching `apps/api/`) cannot stand as evidence the api runs. The session-190 PR body and PR #177 CI rely on this gap not being noticed. Manual runtime probes remain required. | First session that adds a second runtime app (or that converts the saved manifest to a typed recipe) | 2026-09-09 → n/a |
| D54 | **`.cursor/rules/10-stack-and-topology.mdc` TypeORM/PostgreSQL rows were stale against installed reality.** Closed 2026-09-14. Row 24 said `TypeORM | 1.1.x`; `apps/api/package.json` has pinned `typeorm@0.3.20` since D52's scaffold landed (PR #177) — `1.1.x` was aspirational and never installed. Row 25 said `PostgreSQL | 16`; `docker-compose.yml` pins the specific tag `postgres:16.4-alpine`, not the bare major version. D52's own row explicitly flagged this drift as "held back per session 191's 'no other file changes' rule on the docs commit; should be normalised when the next rule-file edit lands." This is that follow-up. **Resolution:** `10-stack-and-topology.mdc` rows 24-25 corrected to `TypeORM | 0.3.20` and `PostgreSQL | 16.4-alpine`, each with a rationale note pointing at the verified source file. `pnpm agents:build` re-ran (`AGENTS.md` regenerated with the same two-row change; `CLAUDE.md` and `60-skills.mdc` unchanged since no skill content moved). | The stack table is docs-of-record for stack decisions; a stale version claim there could mislead a future session into fighting the wrong TypeORM API version or assuming a floating Postgres tag. | None — table now matches `apps/api/package.json` and `docker-compose.yml`. | 2026-09-14 |
| D55 | **`NEXT_PUBLIC_API_URL` is read ad hoc in `sign-in-button.tsx` with no shared config module, and no architecture decision existed for how the web app should reach the API in production.** Huy decided 2026-09-16 (relayed via Echo/Lead in `#corpus-web-v2`): per-environment Vercel var (Option B), not a same-origin rewrites proxy — see **ADR-0004** for full reasoning (matches PR #179's already-locked CORS/cookie-domain contract; avoids re-deriving it through a proxy hop; acceptable build-time-bake tradeoff on a single fixed VPS). **Not yet implemented.** Implementation scope for whoever picks this up: set `NEXT_PUBLIC_API_URL` per Vercel scope (Production/Preview/Development); add one `apps/web/lib/config.ts` export and route every `process.env['NEXT_PUBLIC_API_URL']` read through it instead of scattering the raw env lookup (currently only `sign-in-button.tsx`, but audit is required before assuming that's the only call site); audit for any hardcoded `https://api.nxhhuy.tech` strings and replace with the config import. | Sign-in button's `apiUrl ?? ''` fallback breaks the `disabled` check when the var is unset in a given Vercel scope (session-197 retest finding #1/#2); no single source of truth for the API origin as more client call sites are added. | Assign to `coding-fe` (or `coding-be` if `apps/web/lib/config.ts` needs API-shape awareness) whenever it fits the queue — not urgent, sign-in works today with the dev fallback. **Dev-cwd gotcha (found 2026-09-16, fixed by adding `apps/web/.env.local`, gitignored, not committed):** `NEXT_PUBLIC_API_URL` in the repo-root `.env` is NEVER read by `next dev`/`next build` — Next's own env loader (`@next/env`) only checks `process.cwd()`, which for `apps/web` scripts is `apps/web/`, not the repo root. This is NOT a Turbo strict-mode issue (tested: Turbo passes shell-exported env vars through to child tasks fine; Turbo only gates its own cache-hash inputs, not the runtime env itself). Fix: put a local `apps/web/.env.local` (matches root's `.env.*` gitignore pattern) with `NEXT_PUBLIC_API_URL=http://localhost:3001`. Vercel deploys are unaffected — Vercel injects dashboard env vars directly into the build's `process.env` regardless of monorepo subfolder. If `apps/web/` ever gets wiped/recreated, this file needs to be re-added manually (it can't be committed). | 2026-09-16 |
| D62 | **`.env.example` carries a password-shaped `***` placeholder line that was mistaken for redacted output during session 210 debugging.** Specifically, `.env.example` at the repo root has a `# DATABASE_URL=<postgres://corpus>:***@db:5432/corpus` placeholder; during the 210 hunt for the masked-password bug, this placeholder was copy-pasted into local `.env` files and into mental models — readers assumed `***` was the standard redaction marker, not the actual literal-string bug. The real bug (`toDatabaseUrl()` emitting literal `***` from a `${mask}` interpolation in `apps/api/src/config/env-schema.ts:196` of `2aa6c1d`) was hidden for hours because the placeholder matched the symptom exactly. **Fix is deletion, not substitution:** the `.env.example` placeholder must be deleted; a different redaction marker (or no marker) is the safer shape. (D58 was the original number; append-only IDs forbid reuse, hence D62.) | A future reader reproduces the 210 failure mode by pattern-matching `.env.example`'s `***` placeholder against actual runtime output and concludes the bug is in env-loading, not in `toDatabaseUrl()`. | Delete the placeholder line from `.env.example` (or replace `***` with an unambiguous placeholder like `REDACTED-PASSWORD`). Independent, safe to land — single file edit, no runtime impact, no schema break. | 2026-09-19 |
| D63 | **No CI gate exercises `pnpm --filter @corpus/api start:dev` at all — `verify:api-runtime` only boots the compiled `dist/main.js`.** Found 2026-09-19 during session 210 root-cause analysis: the bug class that produced the `${mask}` → `${password}` regression (DI-under-esbuild dev-mode loader + config-resolution class) is structurally undetectable by the existing gates, because `verify:api-runtime` runs against `dist/main.js` (real `tsc` build, no esbuild) and never touches the dev-mode loader. **Both the DI-under-esbuild class (D57) and the config-resolution class that hid the 210 bug for hours went undetected across all 6 PR #187 CI checks** despite the bug being present at `2aa6c1d` for the full PR window. The structural gap is the absence of any gate that boots `apps/api` under `start:dev` and asserts module wiring + env resolution end-to-end. | Every future bug class that requires the esbuild dev-mode loader + dev-mode env loading (`dotenv.ts` path, `tsx` runtime, module-resolution under `start:dev`) ships to `main`/`develop` undetected. The 210 bug is the first instance; it will not be the last. | Add a CI gate that boots `pnpm --filter @corpus/api start:dev` against a fixture Postgres (could be `postgres:16-alpine` service container or a local throwaway) and asserts: (a) the Nest app reaches `app.listen()`; (b) TypeORM connects to Postgres with the correct password; (c) `/healthz/ready` returns 200; (d) `NODE_ENV=development` and the process is on the dev loader, not `dist/main.js`. Distinct closure condition from D57 (D57 is the specific DI-under-start:dev bug; D63 is the gate that would catch this class going forward). Independent slice. | 2026-09-19 |
| D64 | **New never-violate rule `25-react-provider-event-bus.mdc` — pending Huy sign-off on wording.** Carried over from D58 sub-entry (b): rule text drafted by Lead in PR #185 follow-up 4 — *"Provider-event-bus no-self-trigger rule — When a React provider, context, or hook both _dispatches_ and _listens_ to the same `CustomEvent` (or any pub-sub signal), either (a) a self-vs-external flag exists in `event.detail` AND the listener ignores self-origin, OR (b) the dispatch site is elsewhere — not inside the listener's effective subscription closure. The choice MUST be explicit in the implementation; 'both happen to work' is a bug-shaped coincidence."* Plus Huy's relay-suggested deps-array addition: *"any effect whose deps include state that the same provider updates is suspect — verify the effect's re-fire path can't re-enter its own state setter."* coding-fe's counter-proposal: fold into existing `20-never-violate.mdc` section "Frontend invariants (React)" rather than a new file. **Huy's call** between new `25-react-provider-event-bus.mdc` vs fold-into-`20-never-violate.mdc`, plus final wording sign-off. | Same bug class that produced D58 ships undetected in future PRs if no project-wide rule prevents the pattern. | Decision on file location + final rule text; rule lands in chosen file; CI-regenerated `AGENTS.md` / `.cursor/rules/*.mdc` per `pnpm agents:build` contract. Independent slice. | 2026-09-19 |
| D65 | **Playwright E2E smoke asserting `/me` fires exactly once per `SignInProvider` mount.** Carried over from D58 sub-entry (c): `expect(network).toHaveBeenCalledWith(/\/me/, { count: 1 })` per mount in the chrome-flow E2E suite. Would have caught the D58 bug on the staging deploy before Huy clicked through Vercel preview (Vercel preview testing was visual + manual click-through — no Playwright coverage on `/me` call count). Pending Huy go-ahead to write the spec. | The chrome-flow E2E suite does not assert network call counts; providers that fire network requests on mount can loop silently (the D58 pattern) without test failure. | Add the assertion to `apps/web/test/chrome-flow/` (or wherever the chrome E2E smoke lives in this repo); confirm on Vercel preview that `/me` count is exactly 1 per mount dev+prod (StrictMode dev double-invocation handled by `mountedRef` per `ac8ec24`). Independent slice. | 2026-09-19 |
| D67 | **`loadEnv()` silently calls `loadDotEnv()` and mutates `process.env` — contract violation.** The pre-fix `apps/api/src/config/env-schema.ts:142-147` ran `if (!dotenvLoaded) { await loadDotEnv(defaultEnvCandidates()); dotenvLoaded = true; }` inside `loadEnv()`. `loadDotEnv` → `process.loadEnvFile('.env')`. `loadEnvFile` honors precedence (does NOT overwrite pre-existing values, verified empirically 2026-09-19 with `POSTGRES_PASSWORD=*** node -e "process.loadEnvFile('.env'); console.log(...)"` → output `from-shell`, hex `66726f6d2d7368656c6c`), BUT it fills missing keys from the file. The contract violation: when `withEnv({...})` strips `process.env.DATABASE_URL` before `loadEnv()` runs (the test fixture pattern), `loadDotEnv` re-populates `DATABASE_URL` from `.env` on disk, and `toDatabaseUrl(env)` short-circuits via `env-schema.ts:190-194` returning the file's URL verbatim. **Phase C blast radius:** a `.env` mounted into a Docker container silently overrides injected env config — same silent-fallback family as session 210's four masked-password bugs. **Fix shape: option (f) per Lead's session-215 six-option review**, locked by Huy 2026-09-19 — `loadEnv` becomes pure (no `process.env` mutation, no disk read), new `loadAppEnv()` wrapper does `loadDotEnv → loadEnv` once per process (the `appEnvLoaded` guard lives in the wrapper, NOT in `loadEnv`, per Huy condition #2), and the 5 production call sites (`main.ts:108`, `session.ts:52`, `data-source.ts:27`, `auth.controller.ts:86`, `auth.controller.ts:148`) switch to `loadAppEnv()`. `env-config.ts:10` (`validateEnv`, Nest ConfigModule `validate` hook) stays on `loadEnv` directly because it passes an explicit `rawEnv` argument — that path is what makes the regression guard possible (Huy condition #1: "loadEnv stays exported and stays pure. The test calls it directly with an explicit source — that is what makes the regression guard possible, and it must not become reachable only through the wrapper"). | Every call site that resolves env at runtime (`session.ts` middleware setup, TypeORM `DataSource`, OAuth callback handler, logout handler) carries the same contract violation: an operator who deliberately set `POSTGRES_PASSWORD` (component form) but has a `DATABASE_URL` line in their `.env` will boot against the file's URL, not their explicit component form. Phase C blast radius: container env silently overridden. | Fix lands in this PR (`fix/d67-loadenv-contract-split`); CI `Lint, typecheck, build` includes the api test step, so the new D67 regression test (Case 5: `loadEnv` is pure) gates the fix on every PR. **Not closed by this row:** the per-request `loadEnv` calls in `auth.controller.ts` (lines 86, 148) — even after the `loadAppEnv` wrapper makes the dotenv read once-per-process, the file read + zod parse still runs on every OAuth callback. Structural fix is DI migration (ConfigService or APP_CONFIG provider resolved once at startup). Tracked as **D68**. | 2026-09-19 |
| D68 | **`apps/api` env config resolved per-request from `auth.controller.ts` instead of once at startup.** Found 2026-09-19 while auditing the D67 fix's call sites: `apps/api/src/modules/auth/auth.controller.ts` calls `loadEnv()` (now `loadAppEnv()` after D67) directly at lines 86 and 148 — both inside a single OAuth callback request — rather than going through NestJS DI (`ConfigService` or a custom provider) with the resolved config injected once at boot. **Why this is wrong, not a stylistic preference:** (a) env is bootstrap-time configuration — the file read + zod parse + JSON-clone-on-freeze should happen once at startup, not on every request that happens to need an env value; (b) the config a request sees could in principle differ from the config the app booted with (file contents change between calls → DI returns frozen snapshot from boot, `loadAppEnv()` live-reads the file); (c) `loadAppEnv()` is async; per-request invocation of an async loader on the request hot path is a structurally different shape from "read once at boot, freeze, inject." **D67 mitigation:** the new `loadAppEnv()` wrapper (D67) puts the once-per-process `appEnvLoaded` guard in the wrapper, which prevents the file from being re-read twice per OAuth callback. That closes the immediate cost concern but not the structural one — DI is still the right shape. **Out of scope for D67** (per Huy 2026-09-19): D67 is already a contract change touching 5 production call sites; bundling DI migration in the same PR makes the diff unreadable. Independent slice. **Implementation note for whoever picks it up:** migrate `auth.controller.ts` to inject the resolved config via NestJS DI (either `ConfigService` wired to `loadAppEnv()` at `app.module.ts` registration time, or a custom `APP_CONFIG` provider). After migration, the controller's `loadAppEnv()` calls become synchronous accessors against the frozen boot-time snapshot. Verify `pnpm --filter @corpus/api typecheck`/`lint` clean and a smoke test (real OAuth callback) shows the config resolves at boot and the per-request calls are gone. | Per-request file read + zod parse on the OAuth callback hot path; config-drift window between boot and request in principle; structural mismatch with NestJS DI conventions. | Migrate `auth.controller.ts` to inject the resolved config via NestJS DI (either `ConfigService` wired to `loadAppEnv()` at `app.module.ts` registration time, or a custom `APP_CONFIG` provider). After migration, the controller's `loadAppEnv()` calls become synchronous accessors against the frozen boot-time snapshot. Verify `pnpm --filter @corpus/api typecheck`/`lint` clean and a smoke test (real OAuth callback) shows the config resolves at boot and the per-request calls are gone. Independent slice. | 2026-09-19 |
| D69 | **`/healthz/ready` reports `database: up` against an empty schema.** Discovered 2026-09-19 during the Phase B OAuth round-trip close-out: callback returned `42P01 undefined_table` because the database had zero tables — the volume was reset with `docker compose down -v` during session 210 debugging and nothing since then had written a migration. **Failure mode:** `pnpm --filter api migration:run` had to be run by hand on the live box before the OAuth callback succeeded. **Root cause:** the readiness probe in `apps/api/src/healthz/` (or wherever the `/healthz/ready` handler lives) only checks connectivity (can it SELECT 1?) — it does not assert the `migrations` table exists or that the migrations table is non-empty. Same silent-fallback family as the four from session 210: a probe that says "green" when the underlying resource is misconfigured is worse than no probe, because operators trust it. **Why it wasn't caught earlier:** CI runs `migration:run` before tests, so CI never sees an empty schema. The VPS path skipped that step. **Boot-time migration rejected 2026-09-20 (Huy, PR #193 review):** the proposed `MigrationOnBootstrap` service (`OnApplicationBootstrap` → `runMigrations()`) was rejected with four uncosted consequences — DDL on every PM2 restart with no human gate, retry loops under `autorestart: true`, race the moment a second container runs, and weakening of D69 itself (asserting "schema exists" means little when the app created that schema seconds earlier). Huy's directive: "If you want boot-time migration, argue it as its own decision." Migrations stay an explicit deploy step (`pnpm --filter @corpus/api migration:run` from the operator's terminal). Probe retained as the right shape. See D71. | Operators trust a green readiness probe and skip the schema-must-exist check; OAuth callback 500s on a real user login (reproducible: drop the volume, restart, hit `/auth/google`); debugging cost on the live box is high (touching prod DB through the same `migrations` table the probe should be checking). | Extend `/healthz/ready` to assert that the `migrations` table exists (`SELECT to_regclass('public.migrations') IS NOT NULL`) AND that the row count is > 0. Surface as a separate `database: schema` field in the response JSON, distinct from `database: connection`. **Do not** add a startup-time `migration:run` invocation — migrations are an explicit deploy step. Add a CI gate that wipes the DB before tests and runs the probe — must fail if migrations have not been applied. Independent slice. | 2026-09-19 |
| D70 | **`session.ts:104` runtime-creates `corpus_session` table via `createTableIfMissing: true` — undocumented exception to the `20-never-violate.mdc` `synchronize: true` rule.** Discovered 2026-09-19 during the Phase B OAuth round-trip close-out: `corpus_session` table was created at runtime by `connect-pg-simple` (or the equivalent session store) the first time a session was written, not by a TypeORM migration. **Why this matters:** the table now exists, the schema is implicit, and a future migration set that does not include a `CreateCorpusSession` migration will produce a divergent schema on a fresh box. **Why not reverted:** Huy confirmed `pnpm --filter api migration:run` did not create the table — only the runtime flag did — so removing the flag now would mean re-creating the table by hand on the live box mid-operation. Out-of-band migration is worse than documented exception. **Huy's preference (2026-09-19):** write a migration for `corpus_session` (idempotent `CREATE TABLE IF NOT EXISTS` matching the schema `connect-pg-simple` produced) and turn the `createTableIfMissing` flag off. The flag becomes dead code that can be removed in a follow-up. **Schema-source correction 2026-09-20:** Huy ran `pg_dump --schema-only --table=corpus_session` against the live VPS. Three deltas against the connect-pg-simple-package-inferred DDL shipped in PR #193: (1) constraint name is `session_pkey`, not `corpus_session_pkey` — `ALTER TABLE ADD CONSTRAINT` has no `IF NOT EXISTS`, the migration would RAISE on the live box; (2) index name is `IDX_session_expire`, not `IDX_corpus_session_expire` — `IF NOT EXISTS` saves nothing here, the new index would be a duplicate; (3) column types are `character varying`, `json`, `timestamp(6)` (no length, not `varchar`/`jsonb`). Connect-pg-simple templates the table identifier only; constraint and index names are literal in the package's `table.sql`. PR #193 closed without merge. Corrected slice dispatched to coding-be targeting `fix/d69-d70-corrected`. Huy applies the corrected migration manually on VPS post-merge and posts the receipt. | Future operator forgets the migration exists, runs `migration:run` on a fresh box, gets a divergent schema; or, future maintainer reads `createTableIfMissing: true` and assumes it's a sanctioned pattern (it's not — it's the same family as `synchronize: true`); audit trail gap — the migration history does not record when this table was actually created. **If D70 ships wrong:** `ALTER TABLE ADD CONSTRAINT corpus_session_pkey` raises `42710 duplicate_object` against the live `session_pkey` constraint → MigrationOnBootstrap fails at boot → PM2 `autorestart: true` → infinite boot loop → API down. The corrected migration uses a `DO $$ BEGIN IF NOT EXISTS (...) THEN ALTER TABLE ... ADD CONSTRAINT ...; END IF; END $$;` guard. | Write a TypeORM migration `CreateCorpusSession` that matches the live schema measured via `pg_dump --schema-only --table=corpus_session` from VPS, encode the measured DDL in the migration. Constraint guard via `pg_constraint` lookup or `DO` block (since `ADD CONSTRAINT` has no `IF NOT EXISTS`). Apply it on the VPS (`pnpm --filter api migration:run` — idempotent so safe even though table already exists). Turn `createTableIfMissing: false` in `session.ts:104`. Verify no behavioral change in OAuth round-trip. Test must include `pg_dump` round-trip + idempotency-against-already-applied schema (see D72). Independent slice. | 2026-09-19 |
| D71 | **`MigrationOnBootstrap` boot-time migration hook — REJECTED 2026-09-20.** Huy rejected the `OnApplicationBootstrap` hook that ran `runMigrations()` at app start, citing four uncosted consequences: (1) **DDL on every PM2 restart** with no human gate — a restart that was supposed to be a noop can now touch schema; (2) **retry loops under `autorestart: true`** — a failing migration at boot becomes an infinite boot loop because PM2 keeps restarting; (3) **multi-instance race** — the moment a second container runs, two processes racing `CREATE TABLE IF NOT EXISTS` on the same DB can produce interleaved schema states the migration system was not designed to handle; (4) **weakens D69** — a `/healthz/ready` probe that asserts `migrations` table exists means much less when the app created that schema seconds earlier; the probe no longer distinguishes "schema has been here a while and persisted restarts" from "schema is one migration-old and was just created." Huy's verbatim close: "If you want boot-time migration, argue it as its own decision." **Decision (Lead, 2026-09-20):** migrations stay an explicit deploy step (`pnpm --filter @corpus/api migration:run` from the operator's terminal). The `MigrationOnBootstrap` service, the `DatabaseModule` that registered it, and the `DatabaseModule` import in `app.module.ts` are all dropped in the corrected slice (`fix/d69-d70-corrected`). **Future ask:** if boot-time migration is needed again (Phase C Docker), argue it as a separate decision against these four objections. **Status:** REJECTED 2026-09-20. Implementation removed in the corrected slice PR (the rewritten Slice B). | A future PR reintroducing boot-time migration ships without these objections being argued — the same failure modes recur silently (PM2 boot loop, multi-instance race, weakening of D69). | None — this row documents a rejected decision. If boot-time migration is needed again, the row points at the four objections that must be argued before re-introduction. | 2026-09-20 |
| D72 | **Migration test asserted emitted DDL strings against itself, not against a live schema.** PR #193 shipped `apps/api/test/db/create-corpus-session-migration.test.ts` with 4 cases that asserted the SQL strings the migration code emitted matched the strings the migration code is expected to emit ("code checking the code", Huy 2026-09-20, same class as the trust-proxy assertion rejected on PR #190). The test suite was green while the migration would have failed on the live VPS because the migration's string constants diverged from the real schema on three counts (constraint name `session_pkey`, index name `IDX_session_expire`, column types `character varying`/`json`/`timestamp(6)`). **Concrete failure modes the test missed:** constraint name mismatch → `ALTER TABLE ADD CONSTRAINT` raises `42710 duplicate_object` on the live box, no test caught it; index name mismatch → `IF NOT EXISTS` saves nothing, the new index gets created as a duplicate, no test caught it; column type mismatch (`character varying` vs `varchar`, `json` vs `jsonb`) would have raised, no test caught it. **Decision (Lead, 2026-09-20):** migration tests for future D-rows must include (a) a `pg_dump --schema-only --table=<x>` round-trip — apply the migration to a clean test DB, dump the schema, diff against the literal live DDL string, expect zero diff; AND (b) a second-invocation idempotency test against a DB that already has the schema. Self-asserting DDL string comparisons are insufficient. This rule lands in `20-never-violate.mdc` per D72 follow-through. **Status:** REJECTED 2026-09-20. Replacement test added in the corrected slice PR (the rewritten Slice C). | A future migration test suite is green while the migration would fail on the live box. The same "code checking the code" failure class that produced PR #193's D70 row, PR #190's trust-proxy row, and (separately) the empty-database-behind-a-green-health-check failure repeats silently. | Add a `## Never-violate` rule in `20-never-violate.mdc`: migration tests must include a `pg_dump` round-trip diff + idempotency-against-already-applied schema; self-asserting emitted-string comparisons are insufficient. Rule lands in a small independent PR with Huy sign-off on the wording. Future migrations reference the rule in their PR body. | 2026-09-20 |
| D66 | **Closed** — see `## Closed` below (trust-proxy silent cookie drop on tunnel; fixed same session by `1fd5d06`, merged PR #190 onto develop @ `8d25a0e`). Full narrative preserved in Closed row per append-only rule. | n/a | n/a | 2026-09-19 |

## Closed

| ID | Item | Closed by | Date |
| D66 | **Trust-proxy silent cookie drop on Cloudflare Tunnel — closed 2026-09-19.** Opened 2026-09-19 when the D-7 dispatch landed: `apps/api/src/main.ts` registered `sessionMiddleware` (which builds `cookie: { secure: true, sameSite: 'lax' }` from `SESSION_COOKIE_SECURE=true` default) without first telling Express to trust the upstream proxy. Tunnel terminates TLS at the edge and forwards plain HTTP, so without `trust proxy = 1` Express sees the request as plain HTTP, refuses to set `Secure` cookies (express-session's hard-coded check), and the browser never receives the session row — Google login completes server-side, session is persisted in Postgres, but `/me` returns 401 forever on the next request. Symptom is fully misattributable: every piece of evidence (Google Cloud callback log shows success, `corpus_session` table has the row, `req.isAuthenticated()` ran in the `passport.authenticate` callback) is correct, yet the user is logged out. **Closed by**: `app.getHttpAdapter().getInstance().set('trust proxy', 1)` inserted in `apps/api/src/main.ts` BEFORE the `sessionMiddleware` registration line (`fix/d7-trust-proxy-and-callback` off develop @ `f4152af`, commit `1fd5d06`, merged PR #190 onto develop @ `8d25a0e`). 22-line rationale comment block cites the tunnel-TLS-at-edge topology, the silent-cookie-drop symptom, and the explicit non-derivation of `GOOGLE_CALLBACK_URL` from request-time properties. **Verified unchanged items** (no code change required, confirmed verbatim against files at `f4152af`): `session.ts:79-80` `cookieSecure` reads `SESSION_COOKIE_SECURE` with default `'true'`; `session.ts:81-82` `cookieDomainOrUndef` reads `SESSION_COOKIE_DOMAIN` and converts empty to `undefined`; `session.ts:96` `sameSite: 'lax'` (correct, NOT `'none'` — apex and `api.` share eTLD+1 so Lax is sufficient); `auth.module.ts:71-92` `GOOGLE_CALLBACK_URL` is read at line 71 via `safeParse(process.env)` and passed via `useFactory` to the `GoogleStrategy` constructor at line 92 — literal env read, no derivation from `req.headers.host` / `req.secure` / `req.protocol`. **Huy's VPS-side actions** (not in this row's fix): set `SESSION_COOKIE_DOMAIN=.nxhhuy.tech` in VPS `.env` (line 37, 2026-09-19); verify `/auth/google/callback` succeeds end-to-end with the tunnel in front. **Not fixed here**: supertest-based regression test for the trust-proxy effect — deferred to D63 (gate-coverage work) per "do not trade a dependency for a meaningless assertion" rule. **No VPS claims in PR body** — Huy verifies the trust-proxy effect end-to-end after the tunnel is up, since the behavior is unobservable without TLS in front. | `1fd5d06` (PR #190 → develop @ `8d25a0e`) | 2026-09-19 |
| D13 | **44 `related` refs resolve to nothing — closed 2026-09-04.** Originally 44 refs / 33 distinct targets across `nextjs` and `nestjs`, classified into 4 groups (D13 row, opened 2026-08-16, re-measured 2026-08-17): (1) 4 refs / 2 targets at `nextjs/prompts/{cache-lifetimes,use-cache-directive}.md.tpl`; (2) 21 refs / 16 targets are forward references to concept articles enumerated on the target corpus's roadmap; (3) 19 refs / 15 targets are `nestjs` recipe slugs written into `related` before their track opened; (4) zero rename leftovers. The root `verify-links` gate has been failing on these 44 since the project began (scaffold commit `17a66f1`); every squash-merge to `main` for three weeks has been `--admin` to bypass it. **Closed by `fix/verify-links-roadmap-classification`** (commit forthcoming in this PR), which adds `scripts/lib/roadmap-manifest.mjs`: a per-submodule parser that consults each `roadmap.md` and classifies forward refs into a `plannedTargets` (WARN) bucket. Groups 1, 2, 3 all move to WARN (25 refs total) — the gate no longer fires on roadmap-driven forward refs. **Group 4 (19 refs / 15 distinct nestjs recipe targets) is preserved as the only FAIL.** It is opened as D46 — a real defect (refs to slugs that exist in no manifest anywhere), not a misclassification, and the right shape for the gate to catch. The root cause was never a missing check; it was the absence of a manifest source. The submodule scripts have always consulted their own `roadmap.md`; the root script did not, by deliberate asymmetry ("Cross-repo links WARN in the corpus repos because they cannot resolve standalone. Here they CAN, so here they are fatal"). That asymmetry was the bug. **Branch protection now enforces this going forward (2026-09-08):** `corpus-web` `main` and `develop` both carry `required_status_checks.contexts = ["Content gates", "Lint, typecheck, build", "Repo guards"]`, `strict: false`. A red required check now blocks the merge button on both branches — `--admin` is no longer the routine escape D13's own row describes ("every squash-merge to `main` for three weeks has been `--admin` to bypass it"). Scope: `ci.yml` triggers on `pull_request` (all branches) and `push` to `main` only — `develop` has no non-PR trigger, so this enforces the PR path; a direct push to `develop` bypassing a PR is not itself gated by these checks (though branch protection with required status checks also blocks direct pushes that don't have a matching passing check run, per GitHub's model — not separately verified here). `nestjs-concepts` has no branch protection (still 404 on the protection endpoint) and no CI workflows, so this enforcement is `corpus-web`-only. | `fix/verify-links-roadmap-classification` (this PR) | 2026-09-04 |
|----|------|-----------|------|
| D45 | **`readFileSync` in `article-source.ts:8` — not a defect, misdiagnosis; closed 2026-09-05.** Opened 2026-09-04 under the same D43-shape hypothesis. Closed in session 169 after the user caught that `readArticleMarkdown` is called only from Server Component page handlers (`page.tsx:81`, `lessons/[slug]/page.tsx:88`) wrapped in `'use cache'` + `cacheLife('max')` under `cacheComponents: true`, with both routes emitting all `(locale, corpus, slug)` tuples via `generateStaticParams()` at build time. The fs read runs once per article inside the build container where `content/<repo>/<sourcePath>` submodule gitlinks are reachable; the result is serialized into the prerendered HTML and served as a static file. **Direct prod evidence:** `curl https://nxhhuy.tech/en/blog/nextjs/cache-components-model` returns 724 KB HTML, byte-identical to the local prerendered artifact at `apps/web/.next/server/app/en/blog/nextjs/cache-components-model.html` (724,024 B), containing 51 mentions of "Cache Components model" and 44 mentions of `cacheLife`. No Server Action imports `readArticleMarkdown`. **The agent's session 169 first closed it incorrectly as a "preventive fix" landing PR #162 against develop; the user caught the misdiagnosis before merging and the PR was closed and the branch deleted on the same day.** The fix would also have broken CI on a clean checkout (the `prebuild` hook runs inside `pnpm --filter @corpus/web build` but CI runs `pnpm typecheck` at the root before that, so the gitignored `apps/web/lib/data/article-bodies.ts` module never gets generated → TS2307 "Cannot find module" failure). **Lesson folded into memory and into the session-169 SESSION-LOG row:** "a `readFileSync` inside a `'use cache'` prerender-page handler is build-time by construction, not request-time; the D43 ENOENT signature requires the function to actually be called from a Server Action's request-handling scope. The two surfaces are NOT interchangeable." Action-invocation evidence (D43) does NOT generalise to Server Component page handlers (D45). | Disproven by direct prod probe; row archived. | n/a — no fix required; original speculation was wrong, the function genuinely only runs at build time. | 2026-09-05 |
| D44 | **Cache-boundary action-reference loss — not a defect, misdiagnosis.** Opened 2026-09-04 based on a local `pnpm start` probe of commit `0e2db4c` that returned zero `$F<id>` action references in 114 inline scripts. The hypothesis was that `'use cache'` in `apps/web/lib/article-markdown.tsx:207` captures `<Quiz gradeAction={gradeQuizAnswerForClient}>` (line 390) and drops the action ref from the RSC payload under Next.js 16.3 Cache Components. **Disproved on 2026-09-04 by user-supplied visual confirmation on the deployed Preview URL `https://corpus-7jb9ycfcs-huycong2798s-projects.vercel.app`**: the Quiz grades correctly — `gradeQuizAnswer` returns the right `isCorrect` value, the verdict + explanation render in the browser, and the "Couldn't check that answer" error path is no longer hit. The Vercel runtime log error `ENOENT /var/task/catalog.json, errno: -2, digest: 2812082591` proves the action IS invoked server-side (the 500 with server stack trace rules out the "missing `$F` ref → no server execution" hypothesis the local probe suggested). The local-probe negative result was a Next.js 16.3 dev-mode artefact, not a real defect — dev does not serialise action refs through the same path as production builds. **Closure:** row archived; no fix required. **Moot in practice** since the Quiz works end-to-end on the deployed Preview. **Lessons folded into `prompts/HANDOFF-session-165.md` rule 1** ("local green is not evidence for serverless fs / server actions") — by extension, local-red is not evidence for a defect either, when the deployment shape differs from dev's. | Quiz grades correctly on `develop.nxhhuy.tech`; visual verification confirmed by user 2026-09-04. | n/a — no fix required. | 2026-09-04 |
| D38 | **CI substrate failures exposed by D37 (PR #58, 2026-08-28) — both halves closed.** (a) `verify-links` 44-ref count: superseded by D13 (closed 2026-09-04) → D46 (opened, 19 FAIL / 15 distinct, the real defect). D13's roadmap-classification reclassification (`fix/verify-links-roadmap-classification`, PR #157) is the actual fix; D38(a) tracked a now-obsolete number. (b) `derive-title.test.ts` three-test failure: re-verified 2026-09-04, `node --import tsx --test packages/content-schema/test/derive-title.test.ts` reports 11/11 pass — already fixed in an untracked prior pass. Both halves resolved; row archived per the append-only ID rule (never delete, mark closed). | D13/D46 (verify-links) + untracked content-schema fix (derive-title) | 2026-09-04 |
| D46 | **19 `related` refs in `content/nestjs/*/...md` pointed at recipe slugs that existed in no manifest — closed 2026-09-08.** Opened 2026-09-04 as the group-4 survivor of the D13 reclassification (`verify-links` reported exactly 19 refs / 15 distinct targets, all `nestjs` recipes: `recipes/di-and-modules/nest-cant-resolve-dependencies` × 4, `recipes/validation/nested-dto-not-validated` × 2, plus 13 × 1). **Closed by removing the refs at the source, not by writing the recipes or extending the roadmap manifest** — the three options on this row's own "Fix" column were (a) write the recipe, (b) drop the forward ref, or (c) add a per-recipe-slug manifest; the user picked (b) explicitly: "Do not add a manifest. Do not re-tag them as planned. They come back in the same PR that adds the recipe they point at." `nestjs-concepts#4` (squash-merged into `main` as `216522d`) removed the one dangling `related:` target from each of the 19 source articles. `corpus-web#170` (squash-merged into `develop` as `ea6494a`) bumped the `content/nestjs` submodule gitlink from the pre-D46 pin to `216522d`. **Verified 2026-09-08 against `develop @ ea6494a`:** `pnpm verify:links` reports `445 live edge(s), 0 excluded-target warning(s), 0 draft-target warning(s), 25 planned warning(s), 6 demo warning(s)` and `0 unresolved related ref(s) across all corpora` — the 19-count is gone, not reclassified. **Not closed by this row:** the 45 broken *inline markdown* links (`[text](path)` in article bodies, a disjoint set from `related:` frontmatter) surfaced by `pnpm check:links` in the submodule remain open — see D47. | n/a — refs removed, `verify-links` reports 0 unresolved. | `nestjs-concepts#4` (main @ `216522d`) + `corpus-web#170` (develop @ `ea6494a`) | 2026-09-08 |
| D2 | `websec` is a demo-lab tree, not a markdown corpus — no adapter, never submoduled (removed from PR #1 before merge, session 1 follow-up). | Session 2 | 2026-08-16 |
| D6 | class-validator has defaulted forbidUnknownValues to true since 0.14.0, unconditionally since 0.14.2; from @nestjs/common 9.3.2 ValidationPipe seeds it back to false as an overridable default, so an undecorated DTO that validate() rejects passes silently through the pipe. | `nestjs-concepts@v0.3.2` | 2026-08-19 |
| D12 | `nestjs-concepts` article `validation/dtos-and-class-validator` is now `validation/dtos-and-class-validator.md` and adapts. The six inbound `related` refs (`configuration-and-environment`, `controllers-and-routing`, `pipes`, `serialization-and-response-shaping`, `typescript-for-nest`, `validationpipe-in-depth`) are draft-target warnings, not fatals. The recovered article added one new unresolved outbound ref to `nestjs/nested-dto-not-validated` (D13 group 3, already a target). | `nestjs-concepts@v0.3.1` | 2026-08-17 |
| D14 | *The link report was all-or-nothing, so a real `catalog.json` was unbuildable even once D5 and D11 closed.* Opened by follow-up c, which numbered it D12 — a number D12 above already held. Renumbered here on 2026-08-17; debt IDs are append-only and the earliest claim keeps the number. The link report is now classified four ways: `edges` render, `excludedTargets` and `draftTargets` warn and travel in `catalog.json` for the renderer, and only `unresolvedTargets` — a ref resolving to nothing — is fatal. Of the 128 refs that used to fail, 79 pointed at excluded articles (now warnings, already reported once each by `verify-frontmatter`); the 278 draft-target refs are warnings too. `normaliseStatus()` collapsing object-shaped `status` to `draft` still drives the draft count, and it now costs a link's styling rather than the whole build. | Session 2 follow-up d | 2026-08-16 |
| D18 | **POC accessibility defects** on article chrome. Two of five were already gone (`aria-expanded` on the header toggle; named progress text with `aria-hidden` SVG). Remaining three closed here: labelled sidebar search, announced completed links, `inert` on the closed mobile drawer. | `cursor/fix-d18-a11y-poc-defects` | 2026-08-28 |
| D23 | **Render-mode verification** — `pnpm verify:prerender` asserts against `.next/server/app/**.html`, not the build table, not curl, not view-source. Bracketed `[param]` shells for Cache Components ◐ rows are excluded from the count. | `scripts/verify-prerender.mjs` | 2026-08-19 |
| D39 | `cacheComponents: true` + PPR made Vercel's edge serve an empty `[slug].html` fallback (0 bytes, status 200) for missing-article URLs not in `generateStaticParams()`. Local `pnpm build && pnpm start` reproduced the 500. PR #63 added segment-level + app-wide `not-found.tsx` and `generateMetadata`-notFound — fixed local but Vercel edge served the empty shell before any of those could run (the "before-route resolution" gap). Final fix: middleware-based slug validation in PR #65 (`apps/web/middleware.ts` + `scripts/build-slug-allowlist.mjs` reading `catalog.json` into two static JSON imports). Middleware runs BEFORE Next.js's static routing, returning a real 404 for invalid combinations and bypassing the empty fallback shell entirely. Vercel prod smoke test after PR #66 promotion: `https://nxhhuy.tech/en/blog/react/hooks` returns 404 (was 500); missing lesson URLs also return 404. | PR #66 | 2026-08-28 |
| D40 | `apps/web/components/chrome/search-dialog.tsx` was loading Pagefind 1.x's native-ESM bundle `/pagefind/pagefind.js` via classic `<script src>`. The trailing `export{…}` keyword threw `SyntaxError`, the bundle never registered anything, and the 10s `window.pagefind` poll expired with the misleading "bundle loaded but did not register window.pagefind within 10s" error. Three prior polish PRs (#104, #105, #106) improved error visibility, hardened timeouts, and added loading feedback — none of them addressed the script-tag-vs-ESM-parser mismatch, and the Vercel Auth-SSO 302 hypothesis was a red herring. Fix: replaced the script-tag injection + onload/onerror + 10s-poll dance with a single dynamic `await import('/pagefind/pagefind.js')`. Dynamic import returns the ES module namespace directly — no global registration, no `window.pagefind`, no parse error. Also fixed a secondary bug: replaced the non-existent `pf.getFragment(r, opts?)` call with `r.data()` per Pagefind's canonical API. Collapsed the three-tier error taxonomy into one "Pagefind failed to initialise: <cause>" message. 1 file changed, -70 / +44. Verified end-to-end via Chrome DevTools Protocol on both `pnpm dev` and `pnpm start`: typing "angular" in `/en/courses` ⌘K dialog returns 8 ranked Angular articles with `<mark>angular</mark>` excerpts. | PR #107 | 2026-08-31 |
| D41 | **Film-grain on home hero reads as visual noise** (sydexa-video audit, 2026-09-02). After PR #130 made `.film-grain::after` visible by fixing the stacking-context bug, `.ls-hero film-grain` on `/en` rendered overlay noise over the `from-display to-signal bg-clip-text` H1 and the warm bloom — the same composition the user flagged on `.course-hero` as "dirty CRT screen". **Closed 2026-09-02** via three PRs in the sydexa-video-driven background spec rollout: **(1) course-hero half** via PR #132 — dropped `film-grain` from `.course-hero <header>`, kept both bloom divs; **(2) listing-surface half** via PR #133 — added the new `--ambient-cool-grid` + `--ambient-cool-glow` tokens to `packages/ui/src/tokens.css`, defined the `ls-ambient-grid` + `ls-ambient-glow` CSS modifiers in `apps/web/app/globals.css`, applied them to `.blog-pane` (`/en/blog`) and the new `<section>` wrap (`/en/courses`); **(3) home-hero half** via PR #134 (OPEN as of session-136 wrap, awaiting real-phone spot-check before `--admin` merge) — dropped `film-grain` from `<section className="ls-hero">`, dropped the redundant `bg-signal-dim opacity-25 blur-3xl` JSX bloom div, scrubbed the pre-PR-#133 `repeating-linear-gradient` rail-grid CSS from `.ls-hero`, added `ls-ambient-grid` (28% colour-mix override for ≈8% effective opacity vs the 18% listing-surface default). Kept the vertical surface-tint `linear-gradient(180deg, ...)` canvas gradient (spec §1 Rule 1: "canvas stays the same, only texture layer changed"). Kept both `::before` warm upper-right aurora + `::after` cool lower-left aurora (spec explicitly says keep both). Deliberately omitted `ls-ambient-glow` on `.ls-hero` (the cool `::after` IS the Rule 2 off-center accent). Spec: `prompts/design-spec-2026-08-background.md` (PR #131). | Film-grain + multi-layer bloom composition reads as "dirty CRT" on home and course heroes; listing surfaces had no texture. | Shipped in three PRs of the sydexa-video spec rollout. | 2026-09-02 | Closed 2026-09-02 |
| D58 | **Self-triggering event bus in `SignInProvider` — silent `/me` infinite-loop + masked `fetchMe` `return null` regression.** Bug 1 (loop): `refresh()` dispatched `AUTH_CHANGED_EVENT` while a same-event `useEffect` listener re-called `refresh()`. Bug 2 (masked): `fetchMe()` returned `null` unconditionally before its `try` block. Both bugs in `apps/web/components/chrome/sign-in-context.tsx`, both fixed by PR #185 (`047f545` removes the self-trigger + dead `return null`; `ac8ec24` adds `mountedRef` to break `reactStrictMode` double-mount). Full narrative in `.agents/SESSION-LOG.md` Session 208 and PR #185 follow-up 4 in `CHANGELOG.md`. **Sub-entries (b) and (c) split to D64 and D65 on 2026-09-19 — Huy flagged on PR #187 bookkeeping review that sub-entries buried in the Closed section were exactly the drift this cleanup was supposed to fix.** | `047f545`, `ac8ec24` (both on PR #185) | 2026-09-18 |
| D-5 | **Second Postgres password rotation on VPS — closed 2026-09-19.** Not previously registered in DEBT.md (the ID was used in chat as a narrative handle only); this row establishes it post-hoc with the ID free per append-only rules. D-5 = the second rotation of the production Postgres `corpus_api` 64-hex password, replacing the value exposed in chat/notes on session 210 / D26. Receipts from Huy (verbatim from `#corpus-web-v2` thread): new 64-hex value set on the box, scram verifier confirmed, cold start via `pm2 delete && pm2 start`, `/healthz/ready` returned `{"status":"ok","info":{"database":{"status":"up"}}}` immediately after, `pm2 save` after verify. Live value exists only on the VPS — never appeared in chat, notes, or logs. Rotation unblocks the Cloudflare Tunnel work (D-7 ships first; once D-7 merges, Huy stands up the tunnel and sets `SESSION_COOKIE_DOMAIN=.nxhhuy.tech`). | Second rotation done on VPS, scram-verified, cold-start clean, value stayed on box | 2026-09-19 |
